# 服装电商内容生成平台开发分工与双数据库设计 v1.0

## 1. 文档目的

本文用于第一版项目立项、排期和人员分工，只定义开发范围、系统边界和交付物，不包含代码实现。

参考需求文档：

- `服装电商内容生成平台  PRD v1.0.md`

参考架构：

- `D:\usedCarPlatform` 的业务数据库与积分数据库分离方式

本项目在参考架构基础上进一步收紧边界：

- **业务数据库**：保存用户、企业、套餐、订单、素材、项目、生成任务等全部业务数据。
- **积分数据库**：只管账，只保存积分账户、余额、冻结、结算、退款、过期和积分流水。
- 两个数据库之间不建立跨库外键，不直接跨库 JOIN，通过后端服务接口和业务 ID 关联。

## 2. 第一版范围

### 2.1 第一版需要开发

1. 首页、全局导航、登录注册。
2. AI 在线生成：
   - 衣服图生成。
   - 真人图生成。
   - 人台图生成。
   - AI 模特替换。
3. 视频生成 Beta。
4. AI 创意生图 Beta。
5. 成片交付服务：
   - 服务介绍。
   - 需求提交。
   - 素材上传。
   - 联系销售/客服。
6. 企业版基础页面：
   - 企业能力介绍。
   - 套餐对比。
   - 联系销售。
   - 企业开通申请。
7. 我的项目。
8. 素材管理。
9. 积分余额、积分流水和流水导出。
10. 套餐、充值、支付和会员状态。
11. 异步任务、进度查询、失败退款、并发限制和权限控制。

### 2.2 第一版明确不开发

需求文档中所有标记为 `2.0` 的功能暂不开发：

1. **AI 编辑工具箱 2.0 整个模块**：
   - 智能补光。
   - 智能美化。
   - 魔法擦除。
   - 画质升级。
   - 随心变形。
   - 智能抠图。
   - 编辑预设、批量编辑、撤销/重做等配套能力。
2. 企业版 API 文档 2.0。
3. 企业版多语言 SDK 示例 2.0。
4. 私有化部署 2.0。
5. 企业定制模特服务 2.0。

第一版的任务类型、积分价格、筛选条件和接口中也应删除 `edit` 类型，避免只隐藏页面但后端仍保留半套功能。

### 2.3 原 PRD 已列为范围外

- 短视频文案生成。
- 多平台一键分发。
- CRM 客户管理。
- 供应链/库存管理。
- 3D 实时虚拟试衣间。

## 3. 总体系统边界

建议系统按以下职责拆分：

```text
Web 前端
  |
  v
业务后端 API
  |-- 认证、用户、企业、套餐、订单
  |-- 素材、项目、模特、场景
  |-- 生成任务、队列、AI 服务适配
  |-- 积分服务调用与结果聚合
  |
  |-----------------> 业务数据库
  |
  |-----------------> 积分服务 API ----> 积分数据库
  |
  |-----------------> 对象存储/CDN
  |
  `-----------------> AI 图片/视频服务
```

约束：

- 前端只访问业务后端，不直接访问任何数据库。
- 前端原则上不直接调用积分服务，由业务后端统一鉴权和聚合数据。
- 积分服务不知道用户手机号、昵称、企业名称、套餐名称和任务配置。
- AI 服务回调只能先进入业务后端，由业务后端更新任务并触发积分结算或退款。

## 4. 前端设计与开发分工

### 4.1 产品界面与设计系统

前端/UI 需要先完成：

- 品牌色、字体、间距、圆角、阴影和图标规范。
- 按钮、表单、弹窗、抽屉、筛选器、分页、上传器等基础组件。
- PC 主工作台布局和常用分辨率适配。
- Skeleton、Loading、空状态、错误状态、无权限状态。
- Toast、表单错误、全局 Banner、任务完成通知。
- 图片和视频的加载占位、失败占位及预览交互。

### 4.2 公共页面

- 首页 Landing：
  - Hero、能力入口、案例、数据背书、CTA、咨询入口。
- 全局导航：
  - 登录前后状态。
  - 用户菜单。
  - 套餐、积分、项目和素材入口。
- 登录/注册：
  - 手机验证码登录。
  - 手机号注册。
  - 密码登录与忘记密码。
  - 微信扫码登录。
  - 企业账号登录。
  - 用户协议和隐私政策确认。
- 企业版基础页：
  - 能力介绍、套餐对比、合作平台、咨询表单。
  - 不制作 2.0 的 API 文档、私有化部署和定制模特工作台。

### 4.3 AI 生成工作台

需要为以下任务设计统一工作台框架：

- 衣服图。
- 真人图。
- 人台图。
- AI 模特替换。
- 视频生成。
- AI 创意生图。

统一能力包括：

- 单图/批量上传、拖拽上传、格式和大小预校验。
- 上传进度、失败重试、删除和重新选择。
- 模特库和场景库的浏览、搜索、筛选、收藏。
- 比例、数量、姿态、时长、分辨率等参数配置。
- 实时显示后端返回的积分预估，前端不得自行作为最终计费依据。
- 积分不足、并发超限、队列已满等拦截。
- 任务创建后的排队、处理进度、取消、失败、退款和完成状态。
- WebSocket 实时进度，断开后自动降级为轮询。
- 结果预览、原图对比、下载、批量下载和重新生成。
- 保存到项目。

### 4.4 用户中心

我的项目：

- 任务列表/网格、状态、搜索、筛选和分页。
- 任务详情、输入图、结果图、生成参数、失败原因。
- 重新生成、下载、批量下载和删除。
- 第一版筛选项不出现“编辑”任务。

素材管理：

- 素材上传、文件夹、标签、收藏和删除。
- Logo、参考图、模特、场景、生成结果分类。
- 收藏模特和收藏场景的快速复用。

积分中心：

- 总余额、可用余额、冻结余额、即将过期积分。
- 积分流水、类型筛选、时间筛选和关联任务入口。
- CSV/XLSX 导出。
- 失败任务的退款中、已退款状态。

套餐与支付：

- 当前套餐、有效期、权益和并发额度。
- 套餐对比、月付/年付、充值档位。
- 微信/支付宝支付发起、支付状态轮询和支付结果页。

### 4.5 前端基础工程

- 路由和登录态守卫。
- Access Token 刷新、退出登录和多设备下线处理。
- 统一 API Client、错误码映射和请求追踪 ID 展示。
- 页面权限、按钮权限和企业权限控制。
- 上传队列、任务状态 Store、积分余额 Store。
- 埋点：页面访问、上传、任务创建、生成成功/失败、充值转化。
- XSS 防护、敏感数据不落日志。
- 核心流程的组件测试和端到端测试。

### 4.6 前端交付物

- 页面结构图和可点击原型。
- UI 设计稿与组件规范。
- 页面路由表。
- 前后端接口字段对照表。
- 所有任务状态、空状态和异常状态设计稿。
- 埋点清单。
- 前端验收用例。

## 5. 后端设计与开发分工

### 5.1 认证、用户与企业

- 手机验证码发送、频控和人机校验。
- 注册、密码登录、验证码登录、密码重置。
- 微信扫码授权、账号绑定。
- Access Token、Refresh Token、会话撤销和设备管理。
- 用户资料、账号冻结和注销。
- 企业申请、企业账号、企业成员和角色权限。
- 新用户注册后创建积分账户并发放新人积分，必须保证幂等。

### 5.2 模特、场景和模板

- 模特库、场景库、动作模板、视频模板、创意模板。
- 分类、标签、搜索、筛选、排序和上下架。
- 用户收藏。
- 后台配置入口或最小化运营配置能力。

### 5.3 文件与素材

- 文件真实 MIME、大小、分辨率和安全检查。
- 对象存储上传、缩略图、元数据提取和 CDN 地址。
- 秒传和重复文件识别。
- 批量上传和失败项返回。
- 用户素材、文件夹、标签、收藏和权限隔离。
- 下载临时签名 URL 和下载日志。

第一版可先不实现复杂断点续传，但接口设计应预留上传会话。

### 5.4 生成任务与 AI 服务

- 统一任务模型，支持：
  - `clothes`
  - `real`
  - `mannequin`
  - `model_replace`
  - `video`
  - `creative`
- 参数校验、内容安全和素材归属校验。
- AI 图片/视频供应商适配层，业务代码不直接绑定单一供应商。
- 异步队列、优先级、并发限制、超时和重试。
- 供应商任务 ID、调用记录、回调记录和错误映射。
- 任务状态机、进度更新、WebSocket/SSE 推送。
- 成功后保存结果素材并触发积分结算。
- 失败、超时或符合退款条件的取消任务触发积分退款。
- 重试创建新任务，保留原任务和父子关系，不能覆盖历史记录。

### 5.5 项目与交付服务

- 项目、项目任务、任务详情、搜索和筛选。
- 批量下载包生成。
- 成片交付需求、联系人、交付时间和附件。
- 交付需求状态：已提交、评估中、已确认、制作中、已交付、已关闭。
- 企业咨询和销售线索。

### 5.6 套餐、订单与支付

这些属于业务，不进入积分数据库：

- 套餐定义、价格、权益和并发额度。
- 用户订阅、套餐生效、续费、到期和变更。
- 积分充值商品。
- 支付订单、微信/支付宝支付参数和支付回调。
- 支付回调验签、重复通知幂等。
- 支付成功后调用积分服务入账。
- 套餐赠送、活动赠送和新人赠送均由业务后端触发积分入账。

### 5.7 积分服务

积分服务只负责：

- 创建/停用积分账户。
- 查询总余额、可用余额、冻结余额。
- 赠送、充值入账和过期扣除。
- 按业务任务冻结积分。
- 任务成功后结算。
- 任务失败或取消后释放/退款。
- 查询和导出积分流水。
- 幂等控制、余额并发锁和账务审计。

积分服务不负责：

- 用户登录和用户资料。
- 企业资料和成员关系。
- 套餐名称、套餐价格和套餐页面。
- 支付渠道调用。
- AI 任务执行和任务详情。
- 项目、素材、模特、场景和交付服务。

### 5.8 后端公共能力

- API 版本、统一响应、统一错误码和 `request_id`。
- 参数校验、鉴权、RBAC、限流和审计日志。
- Redis：验证码、限流、短期缓存、分布式锁。
- 消息队列：AI 任务、通知和异步对账。
- 定时任务：任务超时、积分过期、套餐到期、对账修复。
- 日志、指标、告警、链路追踪和管理端审计。
- 单元测试、集成测试、账务一致性测试和并发测试。

### 5.9 后端交付物

- 模块边界和服务架构图。
- API OpenAPI 文档。
- 任务状态机和积分状态机。
- AI 供应商适配协议。
- 消息队列事件定义。
- 错误码字典。
- 权限矩阵。
- 部署配置、监控指标和告警规则。
- 测试报告和对账报告。

## 6. 数据库设计分工

### 6.1 业务数据库

业务数据库是业务事实的唯一来源，建议包含以下表域。

身份与企业：

- `users`
- `user_oauth_bindings`
- `auth_sessions`
- `verification_codes`
- `enterprises`
- `enterprise_members`
- `enterprise_applications`
- `roles`
- `permissions`
- `user_roles`

套餐与支付：

- `plans`
- `plan_prices`
- `plan_features`
- `user_subscriptions`
- `recharge_products`
- `payment_orders`
- `payment_callbacks`

模特、场景和模板：

- `ai_models`
- `model_tags`
- `scenes`
- `scene_tags`
- `action_templates`
- `video_templates`
- `creative_templates`
- `user_model_favorites`
- `user_scene_favorites`

素材与项目：

- `assets`
- `asset_folders`
- `asset_tags`
- `projects`
- `project_items`
- `download_logs`

生成任务：

- `generation_tasks`
- `task_inputs`
- `task_outputs`
- `task_attempts`
- `task_events`
- `batch_tasks`
- `batch_task_items`
- `provider_task_records`

成片交付与咨询：

- `delivery_requests`
- `delivery_request_assets`
- `delivery_status_logs`
- `enterprise_leads`
- `consultation_records`

通知和系统：

- `notifications`
- `operation_logs`
- `outbox_events`
- `system_configs`

`generation_tasks` 中允许保存账务快照和外部引用：

- `estimated_credits`
- `settled_credits`
- `billing_task_id`
- `billing_status`
- `credit_account_id`

这些字段只用于展示、追踪和对账，积分余额与正式流水仍以积分数据库为准。

### 6.2 积分数据库

积分数据库只保存账务数据，建议控制在以下核心表：

- `credit_accounts`
  - 外部账户归属 ID、账户类型、总余额、冻结余额、状态。
- `credit_grants`
  - 每一批赠送/充值积分、原始数量、剩余数量、有效期。
- `billing_tasks`
  - 业务类型、业务 ID、预估积分、冻结积分、结算积分、账务状态。
- `billing_locks`
  - 某次任务冻结了哪些积分批次、冻结数量和过期时间。
- `credit_transactions`
  - 不可变流水：grant、recharge、freeze、settle、refund、expire、adjust。
- `idempotency_keys`
  - 防止注册赠送、支付回调、任务冻结、结算和退款重复执行。
- `ledger_outbox_events`
  - 将账务结果可靠通知业务后端。
- `account_daily_snapshots`（可选）
  - 日终余额快照，用于审计和快速对账。

积分数据库中只保存外部引用：

- `owner_type`
- `owner_id`
- `biz_type`
- `biz_id`
- `source_order_id`

不得复制手机号、昵称、企业名称、套餐详情、订单金额、任务参数和图片地址。

### 6.3 双库关联规则

1. 不使用跨数据库外键。
2. 业务对象 ID 使用 UUID/ULID，并作为积分库的外部字符串引用。
3. 积分库不得直接查询业务库。
4. 业务库不得直接修改积分余额或积分流水。
5. 前端展示积分流水关联任务时，由业务后端先查积分流水，再按 `biz_id` 批量补充任务名称和缩略图。
6. 所有跨库写操作必须带 `idempotency_key`。
7. 每天执行自动对账：
   - 业务任务是否存在对应账务任务。
   - 已完成任务是否已结算。
   - 已失败/已取消任务是否已退款。
   - 支付成功订单是否已入账。
8. 发现不一致时进入补偿队列并告警，禁止人工直接改余额。

## 7. 关键跨库流程

### 7.1 注册赠送

1. 业务库创建用户。
2. 业务后端调用积分服务创建账户。
3. 业务后端以 `register:{user_id}` 为幂等键发放新人积分。
4. 积分库写入积分批次和入账流水。
5. 业务库记录积分账户 ID 和赠送结果。

### 7.2 生成任务扣费

1. 后端根据业务库中的套餐和计价规则计算预估积分。
2. 业务库创建任务，状态为 `billing_pending`。
3. 调用积分服务冻结积分，幂等键使用 `freeze:{task_id}`。
4. 冻结成功后记录 `billing_task_id`，任务进入队列。
5. AI 生成成功后调用结算，幂等键使用 `settle:{task_id}`。
6. AI 生成失败或符合退款条件时调用退款，幂等键使用 `refund:{task_id}`。
7. 业务库保存结算结果快照，积分库保存正式账务记录。

### 7.3 充值入账

1. 业务库创建支付订单。
2. 支付平台回调业务后端。
3. 业务后端验签并将支付订单标记为已支付。
4. 以 `recharge:{payment_order_id}` 为幂等键调用积分服务入账。
5. 积分库写入积分批次和充值流水。
6. 业务库记录入账状态，用于支付订单和账务对账。

## 8. 建议开发顺序

### 阶段一：基础框架

- UI 组件、路由、认证、用户、企业基础模型。
- 两套数据库初始化。
- 积分账户、入账、冻结、结算、退款和幂等。
- 文件上传、对象存储、日志、错误码。

### 阶段二：核心图片生成闭环

- 衣服图、真人图、人台图、模特替换。
- 模特库、场景库、任务队列、进度推送。
- 任务扣费、失败退款、结果保存和下载。

### 阶段三：用户中心与商业闭环

- 我的项目、素材管理。
- 套餐、订阅、充值、支付。
- 积分余额、流水、导出和自动对账。

### 阶段四：扩展能力

- 视频生成 Beta。
- AI 创意生图 Beta。
- 成片交付需求。
- 企业版介绍、申请和联系销售。

### 阶段五：稳定性与上线

- 权限、安全、内容审核和限流。
- 性能压测、并发测试、账务一致性测试。
- 监控告警、数据备份、灰度发布和上线验收。

## 9. 开发前必须确认的问题

1. 新人福利存在冲突：
   - PRD 一处写“10 张免费作图额度”。
   - 其他章节写“40 张免费额度”。
   - 开发前必须确定最终赠送积分数、有效期和对应图片数量。
2. AI 图片和视频实际供应商、模型、接口限制和成本尚未明确。
3. 视频生成和创意生图虽为 Beta，但是否必须随第一版正式上线需要确认。
4. 衣服图、真人图、人台图和模特替换的真实计费单位、失败退款规则需要最终确认。
5. 任务在“已经开始处理后由用户取消”时是否退款，PRD 当前规则不够明确。
6. 成片交付需要配套内部处理后台，否则只能收集表单，不能形成完整交付闭环。
7. 企业账号由谁开户、企业成员上限和管理员权限需要确认。
8. 微信登录、短信、微信支付和支付宝支付需要提前准备正式商户资质和回调域名。
9. 图片、视频保存时长、用户删除后的物理清理周期需要确认。
10. PRD 中的 ArcMuse/PSAI 为参考产品表述，本项目正式上线文案和实际 AI 引擎名称需要替换。

## 10. 分工结论

- **前端团队**负责用户看到和操作的完整流程，以及任务、积分、支付和异常状态的准确呈现。
- **后端业务团队**负责用户、企业、素材、项目、AI 任务、套餐、订单、支付和统一 API。
- **积分服务团队**只负责可靠记账，不承载业务资料和业务流程。
- **数据库负责人**负责两套库的模型、索引、迁移、备份、审计、对账和数据生命周期。
- **AI/算法接入负责人**负责模型供应商适配、参数映射、任务回调、效果和成本评估。
- **测试负责人**重点验证任务状态机、重复请求幂等、支付入账、积分冻结/结算/退款和跨库对账。
