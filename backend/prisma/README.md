# Prisma Layout

This directory stores the database baseline for the backend workspace.

Layout:

- `biz/` business database schema
- `ledger/` credit database schema
- `migrations/` reserved for future migration output

Current stage:

- schema baseline only
- no generated client committed
- no live migration files yet

