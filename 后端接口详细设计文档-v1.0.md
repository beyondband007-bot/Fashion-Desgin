# 服装电商内容生成平台后端接口详细设计文档 v1.0

## 1. 文档目标

本文定义第一版业务后端和积分服务的模块边界、接口清单、请求响应、状态机、幂等和安全要求。

说明：

- 本文是项目研发所需的内部接口设计，不属于 PRD 中面向企业客户的“API 文档 2.0”功能。
- 第一版不实现 AI 编辑工具箱，接口和枚举中禁止出现可用的 `edit` 任务。
- 前端只调用业务后端；积分服务仅允许业务后端和内部管理任务调用。

## 2. 服务边界

### 2.1 业务后端

负责：

- 认证、用户、企业、权限。
- 模特、场景、模板。
- 上传、素材、项目。
- AI 任务、队列、供应商适配、回调。
- 套餐、订阅、充值商品、支付订单。
- 成片交付、咨询、通知。
- 聚合积分余额和积分流水。

### 2.2 积分服务

只负责：

- 积分账户。
- 积分批次和有效期。
- 入账、冻结、结算、退款、过期和人工调账。
- 账务流水、幂等、快照和对账事件。

积分服务不保存用户资料、企业资料、套餐定义、支付金额、任务参数和素材地址。

## 3. 通用接口规范

### 3.1 基础约定

|项目|约定|
|---|---|
|协议|HTTPS|
|业务 API 前缀|`/api/v1`|
|内部积分 API 前缀|`/internal/v1`|
|数据格式|JSON，上传接口除外|
|字符集|UTF-8|
|时间|ISO 8601，统一保存 UTC，返回带时区|
|ID|UUID v7 或 ULID，API 中使用字符串|
|分页|`page` 从 1 开始，`page_size` 默认 20，最大 100|
|认证|`Authorization: Bearer {access_token}`|

### 3.2 通用请求头

|请求头|必填|说明|
|---|---|---|
|`Authorization`|登录接口外必填|Access Token|
|`X-Request-Id`|否|客户端请求 ID，不传由服务端生成|
|`Idempotency-Key`|写入类关键接口必填|防止重复请求|
|`X-CSRF-Token`|Cookie 会话敏感操作必填|防 CSRF|
|`Accept-Language`|否|默认 `zh-CN`|

### 3.3 通用成功响应

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "request_id": "req_01J..."
}
```

分页响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "items": [],
    "page": 1,
    "page_size": 20,
    "total": 0,
    "has_more": false
  },
  "request_id": "req_01J..."
}
```

### 3.4 通用错误响应

```json
{
  "code": 300001,
  "message": "积分不足",
  "data": {
    "required": 40,
    "available": 25
  },
  "request_id": "req_01J..."
}
```

错误码域：

|范围|模块|
|---|---|
|`100000-199999`|通用参数、鉴权、权限|
|`200000-299999`|认证、用户、企业|
|`300000-399999`|积分、套餐、支付|
|`400000-499999`|任务、项目、AI 服务|
|`500000-599999`|上传、素材、内容安全|
|`600000-699999`|交付、通知|
|`900000-999999`|系统、依赖服务|

## 4. 核心数据对象

### 4.1 UserSummary

```json
{
  "id": "usr_xxx",
  "phone_masked": "138****8000",
  "nickname": "用户8000",
  "avatar_url": null,
  "user_type": "personal",
  "status": "active",
  "enterprise": null,
  "permissions": ["task:create", "asset:read"],
  "credit_account_ready": true
}
```

### 4.2 TaskSummary

```json
{
  "id": "tsk_xxx",
  "name": "衣服图生成 2026-06-09",
  "type": "clothes",
  "status": "processing",
  "progress": 52,
  "stage": "generating",
  "cover_url": "https://cdn.example.com/...",
  "estimated_credits": 40,
  "settled_credits": null,
  "billing_status": "frozen",
  "queue_position": null,
  "cancellable": false,
  "created_at": "2026-06-09T12:00:00+08:00",
  "updated_at": "2026-06-09T12:01:00+08:00"
}
```

### 4.3 CreditBalance

```json
{
  "account_id": "acc_xxx",
  "total": 1000,
  "available": 800,
  "frozen": 200,
  "expiring_soon": 50,
  "expiring_at": "2026-06-16T23:59:59+08:00",
  "updated_at": "2026-06-09T12:00:00+08:00"
}
```

## 5. 认证接口

### 5.1 发送短信验证码

`POST /api/v1/auth/sms/send`

鉴权：否。限流：手机号 60 秒 1 次、24 小时 10 次；IP 每小时 20 次。

请求：

|字段|类型|必填|说明|
|---|---|---|---|
|`phone`|string|是|中国大陆手机号|
|`purpose`|enum|是|`login/register/reset_password/bind_phone`|
|`captcha_token`|string|条件必填|触发图形验证码后必填|

响应：

```json
{
  "expire_in": 300,
  "resend_after": 60,
  "verification_id": "ver_xxx"
}
```

### 5.2 注册

`POST /api/v1/auth/register`

请求头：`Idempotency-Key` 必填。

请求：

|字段|类型|必填|说明|
|---|---|---|---|
|`phone`|string|是|手机号|
|`verification_id`|string|是|验证码会话 ID|
|`code`|string|是|6 位验证码|
|`password`|string|是|8-20 位|
|`nickname`|string|否|2-20 字|
|`invite_code`|string|否|邀请码|
|`agreement_version`|string|是|用户协议版本|
|`privacy_version`|string|是|隐私政策版本|

事务边界：

1. 业务库创建用户和会话。
2. 通过 Outbox/补偿机制创建积分账户并赠送新人积分。
3. 返回前确认 `credit_account_ready`；超时可返回初始化中状态。

响应：

```json
{
  "user": {},
  "access_token": "token",
  "expires_in": 1800,
  "credit_initialization": "completed"
}
```

### 5.3 登录

`POST /api/v1/auth/login`

请求：

|字段|类型|必填|说明|
|---|---|---|---|
|`login_type`|enum|是|`sms/password/enterprise_password`|
|`phone`|string|条件必填|手机登录|
|`verification_id`|string|条件必填|短信登录|
|`code`|string|条件必填|短信登录|
|`password`|string|条件必填|密码登录|
|`enterprise_code`|string|条件必填|企业账号登录|
|`device_name`|string|否|设备名称|

响应：用户、Access Token、过期时间；Refresh Token 写入 `httpOnly` Cookie。

### 5.4 刷新 Token

`POST /api/v1/auth/token/refresh`

请求：Refresh Token Cookie。

响应：

```json
{
  "access_token": "token",
  "expires_in": 1800
}
```

Refresh Token 必须轮换，旧 Token 立即失效。

### 5.5 退出登录

`POST /api/v1/auth/logout`

请求：

|字段|类型|必填|说明|
|---|---|---|---|
|`all_devices`|boolean|否|默认 `false`|

### 5.6 重置密码

`POST /api/v1/auth/password/reset`

请求：手机号、验证码会话、验证码、新密码。

规则：新密码不可与最近 3 次密码相同。

### 5.7 微信扫码

- `POST /api/v1/auth/wechat/qrcode`
- `GET /api/v1/auth/wechat/status?state={state}`
- `GET /api/v1/auth/wechat/callback`

状态：`pending/scanned/authorized/success/expired/cancelled`。

`state` 单次使用，有效期 60 秒，并绑定浏览器会话防 CSRF。

### 5.8 会话管理

- `GET /api/v1/auth/sessions`
- `DELETE /api/v1/auth/sessions/{session_id}`
- `DELETE /api/v1/auth/sessions`：退出其他设备。

## 6. 用户与企业接口

### 6.1 当前用户

- `GET /api/v1/users/me`
- `PATCH /api/v1/users/me`
- `POST /api/v1/users/me/avatar`
- `POST /api/v1/users/me/password/change`
- `POST /api/v1/users/me/phone/change`

更新资料请求：

|字段|类型|说明|
|---|---|---|
|`nickname`|string|2-20 字，30 天内最多修改一次|
|`email`|string|null 表示解绑|
|`avatar_asset_id`|string|必须属于当前用户|

### 6.2 企业申请

- `POST /api/v1/enterprise/applications`
- `GET /api/v1/enterprise/application`
- `PATCH /api/v1/enterprise/application`

创建请求：

|字段|类型|必填|
|---|---|---|
|`enterprise_name`|string|是|
|`contact_name`|string|是|
|`contact_phone`|string|是|
|`contact_email`|string|否|
|`industry`|string|否|
|`employee_scale`|string|否|
|`expected_usage`|string|是|
|`business_license_asset_id`|string|否|

状态：`draft/submitted/reviewing/approved/rejected/cancelled`。

### 6.3 企业成员

- `GET /api/v1/enterprise/members`
- `POST /api/v1/enterprise/members`
- `PATCH /api/v1/enterprise/members/{member_id}`
- `DELETE /api/v1/enterprise/members/{member_id}`

仅企业管理员可操作。成员删除不删除用户，只解除企业关系。

## 7. 公共配置接口

- `GET /api/v1/public/home-config`
- `GET /api/v1/public/cases`
- `GET /api/v1/public/system-status`
- `GET /api/v1/public/agreements/{type}`

后台内容应从业务库配置，不硬编码在前端。

## 8. 上传与素材接口

### 8.1 秒传检查

`POST /api/v1/uploads/check`

请求：

|字段|类型|必填|
|---|---|---|
|`sha256`|string|是|
|`file_name`|string|是|
|`size`|integer|是|
|`purpose`|enum|是|

`purpose`：`clothes/real/mannequin/video_input/creative_input/logo/reference/delivery`。

响应：

```json
{
  "exists": true,
  "asset": {
    "id": "ast_xxx",
    "url": "https://...",
    "thumbnail_url": "https://..."
  }
}
```

秒传仅在当前用户有权访问同一物理文件时返回可用资产。

### 8.2 单文件上传

`POST /api/v1/uploads`

内容类型：`multipart/form-data`。

字段：

- `file`
- `purpose`
- `folder_id` 可选

服务端处理：

1. 限制大小。
2. 校验 magic number。
3. 病毒和内容安全检测。
4. 提取宽高、时长、格式。
5. 生成缩略图。
6. 上传对象存储。
7. 写入资产记录。

响应：`asset_id/url/thumbnail_url/media_type/width/height/duration/size/review_status`。

### 8.3 批量上传

`POST /api/v1/uploads/batch`

最多 20 个文件。响应分别返回成功项和失败项，单个失败不回滚全部。

### 8.4 素材 CRUD

- `GET /api/v1/assets`
- `GET /api/v1/assets/{id}`
- `PATCH /api/v1/assets/{id}`
- `DELETE /api/v1/assets/{id}`
- `POST /api/v1/assets/{id}/favorite`
- `DELETE /api/v1/assets/{id}/favorite`

列表参数：

|参数|说明|
|---|---|
|`page/page_size`|分页|
|`media_type`|`image/video/file`|
|`purpose`|用途|
|`folder_id`|文件夹|
|`favorite`|是否收藏|
|`keyword`|名称搜索|
|`start_time/end_time`|时间范围|

更新字段：`name/folder_id/tags`。

### 8.5 文件夹

- `GET /api/v1/asset-folders`
- `POST /api/v1/asset-folders`
- `PATCH /api/v1/asset-folders/{id}`
- `DELETE /api/v1/asset-folders/{id}`

删除非空文件夹时必须指定 `move_to_folder_id` 或 `delete_assets=false`。

## 9. 模特、场景和模板接口

### 9.1 模特

- `GET /api/v1/models`
- `GET /api/v1/models/{id}`
- `POST /api/v1/models/{id}/favorite`
- `DELETE /api/v1/models/{id}/favorite`

列表参数：

- `race`
- `age_group`
- `style`
- `body_type`
- `skin_tone`
- `hair_color`
- `keyword`
- `sort=popular/latest/favorites`
- `favorite_only`

### 9.2 场景

- `GET /api/v1/scenes`
- `GET /api/v1/scenes/{id}`
- `POST /api/v1/scenes/{id}/favorite`
- `DELETE /api/v1/scenes/{id}/favorite`

列表参数：`category/tone/lighting/keyword/sort/favorite_only`。

### 9.3 模板

- `GET /api/v1/templates/actions`
- `GET /api/v1/templates/videos`
- `GET /api/v1/templates/creative`

返回模板参数约束、支持的任务类型和预览素材。

## 10. 任务接口

### 10.1 任务类型

```text
clothes
real
mannequin
model_replace
video
creative
```

### 10.2 获取计费预估

`POST /api/v1/tasks/estimate`

请求：

```json
{
  "type": "clothes",
  "input_asset_ids": ["ast_1", "ast_2"],
  "model_id": "mdl_xxx",
  "scene_id": "scn_xxx",
  "config": {
    "ratio": "3:4",
    "pose": "standing",
    "output_count": 2,
    "resolution": "1K",
    "lock_model": true
  }
}
```

响应：

```json
{
  "quote_id": "qte_xxx",
  "unit_credits": 10,
  "billable_quantity": 4,
  "estimated_credits": 40,
  "available_credits": 100,
  "sufficient": true,
  "expires_at": "2026-06-09T12:05:00+08:00",
  "pricing_snapshot": {
    "plan_code": "basic",
    "rule_version": "2026-06-01"
  }
}
```

预估仅由后端计算，报价默认 5 分钟有效。

### 10.3 创建任务

`POST /api/v1/tasks`

请求头：`Idempotency-Key` 必填。

请求：

|字段|类型|必填|说明|
|---|---|---|---|
|`quote_id`|string|是|未过期报价|
|`type`|enum|是|任务类型|
|`name`|string|否|任务名称|
|`input_asset_ids`|string[]|是|已审核资产|
|`model_id`|string|按类型|模特|
|`scene_id`|string|按类型|场景|
|`template_id`|string|按类型|视频/创意模板|
|`project_id`|string|否|所属项目|
|`config`|object|是|任务参数|

处理顺序：

1. 校验报价、资源归属、权限和并发。
2. 业务库创建 `billing_pending` 任务。
3. 调用积分服务冻结积分。
4. 冻结成功后更新 `billing_status=frozen` 并写入队列。
5. 冻结失败时任务标记 `billing_failed`，不进入 AI 队列。

响应：

```json
{
  "task": {},
  "credit_balance": {},
  "ws_channel": "user:usr_xxx:tasks"
}
```

### 10.4 任务列表与详情

- `GET /api/v1/tasks`
- `GET /api/v1/tasks/{id}`

列表参数：

- `type`
- `status`
- `billing_status`
- `project_id`
- `keyword`
- `start_time/end_time`
- `page/page_size`

详情包含：

- 输入、输出。
- 完整配置。
- 供应商调用摘要，不返回密钥和内部敏感响应。
- 任务事件。
- 账务快照。
- `cancellable/retryable/downloadable`。

### 10.5 取消任务

`POST /api/v1/tasks/{id}/cancel`

请求头：`Idempotency-Key` 必填。

请求：

```json
{
  "reason": "user_cancelled"
}
```

后端根据实际阶段决定：

- 尚未调用供应商：取消并全额退款。
- 供应商支持取消且取消成功：按规则退款。
- 已不可取消：返回 `400002` 和当前状态。

### 10.6 重试任务

`POST /api/v1/tasks/{id}/retry`

创建新任务，保留：

- `parent_task_id`
- 原配置快照
- 原任务失败原因

重试必须重新报价和冻结积分；如果属于系统自动重试则不重复收费。

### 10.7 下载

- `POST /api/v1/tasks/{id}/download`
- `POST /api/v1/tasks/batch-download`
- `GET /api/v1/download-packages/{id}`

下载响应返回 1 小时内有效的签名地址。批量下载异步生成 ZIP。

### 10.8 删除任务

`DELETE /api/v1/tasks/{id}`

仅软删除用户侧展示记录，不删除账务、审计和供应商调用记录。

### 10.9 任务进度通道

- `POST /api/v1/realtime/token`：获取短期 WebSocket Token。
- `GET /api/v1/ws?token={short_lived_ws_token}`：建立 WebSocket 连接。
- WebSocket 事件：`task.created/task.updated/task.completed/task.failed/task.refunded`。

## 11. AI 供应商内部接口

业务后端应定义统一适配器：

```text
estimateCapability()
submitTask()
getTaskStatus()
cancelTask()
normalizeResult()
normalizeError()
verifyCallback()
```

供应商回调：

`POST /api/v1/provider-callbacks/{provider}`

要求：

- 验签。
- 原始回调落审计表。
- 以 `provider + provider_task_id + event_id` 幂等。
- 不信任供应商传入的本地任务 ID，必须通过映射表查询。
- 回调只更新业务任务；结算/退款由业务后端调用积分服务。

## 12. 项目接口

- `GET /api/v1/projects`
- `POST /api/v1/projects`
- `GET /api/v1/projects/{id}`
- `PATCH /api/v1/projects/{id}`
- `DELETE /api/v1/projects/{id}`
- `POST /api/v1/projects/{id}/items`
- `DELETE /api/v1/projects/{id}/items/{task_id}`
- `POST /api/v1/projects/{id}/download`

项目创建字段：

|字段|类型|必填|
|---|---|---|
|`name`|string|是|
|`description`|string|否|
|`cover_asset_id`|string|否|

项目删除默认不删除任务，只解除项目关系。

## 13. 积分聚合接口

以下由业务后端鉴权后调用积分服务。

### 13.1 余额

`GET /api/v1/credits/balance`

响应：总余额、可用、冻结、即将过期和更新时间。

### 13.2 流水

`GET /api/v1/credits/transactions`

参数：

- `transaction_type`
- `status`
- `biz_type`
- `start_time/end_time`
- `page/page_size`

业务后端按 `biz_id` 补充任务名称、缩略图或订单号。

### 13.3 导出

`POST /api/v1/credits/transactions/export`

请求：

```json
{
  "start_time": "2026-06-01T00:00:00+08:00",
  "end_time": "2026-06-30T23:59:59+08:00",
  "transaction_type": null,
  "format": "xlsx"
}
```

导出任务异步执行，完成后通过通知和短期下载地址交付。

## 14. 套餐、订阅和支付接口

### 14.1 套餐

- `GET /api/v1/plans`
- `GET /api/v1/subscriptions/current`
- `GET /api/v1/subscriptions/history`

套餐返回：

- 价格。
- 周期。
- 每月赠送积分。
- 单图、批量和视频并发。
- 功能权限。
- 生效版本。

### 14.2 创建套餐订单

`POST /api/v1/subscriptions/orders`

请求头：`Idempotency-Key`。

请求：

|字段|类型|必填|
|---|---|---|
|`plan_id`|string|是|
|`billing_cycle`|enum|`monthly/yearly`|
|`payment_channel`|enum|`wechat/alipay`|
|`return_url`|string|否|

### 14.3 充值商品

`GET /api/v1/recharge-products`

商品定义保存在业务库，积分服务不保存商品名称和人民币价格。

### 14.4 创建支付订单

`POST /api/v1/payment-orders`

请求头：`Idempotency-Key`。

请求：

```json
{
  "order_type": "credit_recharge",
  "product_id": "prd_xxx",
  "payment_channel": "wechat",
  "return_url": "https://..."
}
```

响应：

```json
{
  "order_id": "ord_xxx",
  "order_no": "20260609...",
  "amount": "99.00",
  "currency": "CNY",
  "status": "paying",
  "payment": {
    "type": "qrcode",
    "qrcode_url": "weixin://..."
  },
  "expire_at": "2026-06-09T12:30:00+08:00"
}
```

### 14.5 查询和关闭支付订单

- `GET /api/v1/payment-orders/{id}`
- `POST /api/v1/payment-orders/{id}/close`

### 14.6 支付回调

- `POST /api/v1/payment-callbacks/wechat`
- `POST /api/v1/payment-callbacks/alipay`

流程：

1. 验签。
2. 保存原始回调。
3. 锁定支付订单。
4. 幂等更新为已支付。
5. 创建订阅或调用积分服务入账。
6. 入账成功后订单状态更新为 `completed`。

支付成功和积分入账必须分成两个状态，便于补偿。

## 15. 成片交付与咨询接口

### 15.1 销售线索

`POST /api/v1/leads`

请求：来源、姓名、手机号、企业、需求摘要和期望联系时间。

### 15.2 成片交付

- `POST /api/v1/delivery-requests`
- `GET /api/v1/delivery-requests`
- `GET /api/v1/delivery-requests/{id}`
- `POST /api/v1/delivery-requests/{id}/cancel`

创建字段：

- 标题。
- 描述。
- 用途。
- 风格标签。
- 期望交付时间。
- 企业和联系人。
- 附件资产 ID。

状态：

```text
submitted
evaluating
proposal_pending
confirmed
producing
delivered
closed
cancelled
```

## 16. 通知接口

- `GET /api/v1/notifications`
- `GET /api/v1/notifications/unread-count`
- `POST /api/v1/notifications/{id}/read`
- `POST /api/v1/notifications/read-all`

通知类型：

- 任务完成/失败/退款。
- 积分即将过期。
- 支付和套餐。
- 企业申请。
- 成片交付状态。

## 17. 内部积分服务接口

所有接口使用服务身份认证、IP/网络隔离、请求签名和 `Idempotency-Key`。

### 17.1 创建账户

`POST /internal/v1/accounts`

请求：

```json
{
  "owner_type": "user",
  "owner_id": "usr_xxx",
  "currency": "CREDIT"
}
```

响应：`account_id/status/total_balance/locked_balance`。

唯一约束：`owner_type + owner_id + currency`。

### 17.2 查询账户

- `GET /internal/v1/accounts/by-owner?owner_type=user&owner_id=usr_xxx`
- `GET /internal/v1/accounts/{account_id}/balance`

### 17.3 入账

`POST /internal/v1/accounts/{account_id}/grants`

适用于注册赠送、套餐赠送、活动赠送和充值。

请求：

|字段|类型|必填|
|---|---|---|
|`grant_type`|enum|是|
|`points`|decimal|是，必须大于 0|
|`biz_type`|string|是|
|`biz_id`|string|是|
|`source_order_id`|string|否|
|`expire_at`|datetime|否|
|`description_code`|string|是|

### 17.4 冻结积分

`POST /internal/v1/billing-tasks/freeze`

请求：

```json
{
  "account_id": "acc_xxx",
  "biz_type": "generation_task",
  "biz_id": "tsk_xxx",
  "estimated_points": 40,
  "pricing_snapshot": {
    "rule_version": "2026-06-01",
    "plan_code": "basic"
  },
  "lock_expire_at": "2026-06-09T12:30:00+08:00"
}
```

响应：

```json
{
  "billing_task_id": "blt_xxx",
  "status": "frozen",
  "frozen_points": 40,
  "balance": {}
}
```

### 17.5 结算

`POST /internal/v1/billing-tasks/{id}/settle`

请求：

|字段|类型|必填|
|---|---|---|
|`actual_points`|decimal|是|
|`result`|enum|是，`success/partial_success`|
|`description_code`|string|是|

实际积分小于冻结积分时释放差额；不得大于冻结积分，若需补扣必须走独立追加计费流程。

### 17.6 退款/释放

`POST /internal/v1/billing-tasks/{id}/refund`

请求：

```json
{
  "points": 40,
  "reason_code": "provider_failed",
  "reason_detail": "timeout"
}
```

默认全额释放未结算冻结；部分退款必须明确金额。

### 17.7 查询账务任务

- `GET /internal/v1/billing-tasks/{id}`
- `GET /internal/v1/billing-tasks/by-biz?biz_type=generation_task&biz_id=tsk_xxx`

### 17.8 查询流水

`GET /internal/v1/accounts/{account_id}/transactions`

### 17.9 过期处理

`POST /internal/v1/grants/expire`

仅定时任务调用，按批次锁定和扣除剩余积分。

### 17.10 人工调账

`POST /internal/v1/accounts/{account_id}/adjustments`

必须包含：

- 调整方向和积分。
- 原因码。
- 工单号。
- 操作管理员。
- 审批管理员。

禁止直接执行 SQL 修改余额。

## 18. 任务状态机

业务状态：

```text
created
  -> billing_pending
  -> queued
  -> processing
  -> completed
  -> failed
  -> cancelling
  -> cancelled
```

账务状态：

```text
not_started
  -> freezing
  -> frozen
  -> settling -> settled
  -> refunding -> refunded
  -> failed
```

要求：

- 业务状态和账务状态分字段保存。
- `completed` 必须最终对应 `settled`。
- 可退款的 `failed/cancelled` 必须最终对应 `refunded`。
- 账务调用失败不丢任务，通过 Outbox 和补偿任务重试。

## 19. 幂等与一致性

必须幂等的场景：

- 注册。
- 创建任务。
- 取消和重试任务。
- 创建支付订单。
- 支付回调。
- 注册赠送、套餐赠送和充值入账。
- 冻结、结算、退款、过期。
- AI 供应商回调。

实现要求：

- 幂等键与调用方、接口、请求摘要绑定。
- 相同键、不同请求摘要返回 `409`。
- 已成功请求直接返回原结果。
- 正在处理返回可查询状态，不重复执行。
- 跨库使用本地事务 + Outbox + 重试，不使用跨库分布式事务。

## 20. 定时任务与补偿

|任务|建议频率|作用|
|---|---|---|
|任务超时扫描|每分钟|标记超时、尝试取消和退款|
|供应商状态补查|30 秒|补偿回调丢失|
|支付订单补查|每分钟|补偿支付回调丢失|
|积分入账补偿|每分钟|支付成功但未入账|
|账务状态补偿|每分钟|冻结/结算/退款失败重试|
|积分过期|每小时|处理到期批次|
|套餐到期|每小时|更新订阅状态|
|双库对账|每日|任务、支付和积分一致性|
|孤儿对象清理|每日|清理未绑定上传文件|

## 21. 安全与审计

- 密码使用 Argon2id 或 bcrypt 12+。
- 手机、邮箱 AES-256-GCM 加密，另存不可逆哈希用于查询。
- Access Token 使用非对称签名，短期有效。
- Refresh Token 只保存哈希。
- 文件检查真实类型、病毒和内容安全。
- 所有资源查询带 `user_id/enterprise_id` 归属条件。
- 管理操作记录操作者、目标、前后值、IP、User-Agent 和 `request_id`。
- 积分流水不可更新和删除，只允许追加冲正流水。
- 支付和积分日志至少按合规周期保存。

## 22. 测试要求

必须覆盖：

1. 注册重复请求只创建一个用户和一笔赠送。
2. 同一任务重复创建只冻结一次积分。
3. 余额不足时不创建可执行任务。
4. 供应商重复回调只结算一次。
5. 任务失败重复通知只退款一次。
6. 支付重复回调只入账一次。
7. 结算、退款并发时只有合法状态转换成功。
8. 用户不可读取或操作他人资源。
9. 业务库和积分库断连后的补偿恢复。
10. 所有接口拒绝 `edit` 任务类型。

## 23. 待确认项

- 新人赠送积分数量和有效期。
- AI 供应商及各任务参数映射。
- 处理中取消的退款比例。
- 视频和创意生图的正式计费规则。
- 套餐升级、降级和未使用权益处理。
- 企业账户使用个人积分账户还是企业共享账户。
- 支付退款是否同步扣回已赠积分。
