# 服装电商内容生成平台后端开发技术方案（TS）v1.0

## 1. 文档目标

本文基于以下资料形成后端开发技术方案，不包含代码实现：

- `后端接口详细设计文档-v1.0.md`
- `服装电商内容生成平台  PRD v1.0.md`
- `前端详细设计文档-v1.0.md`
- `数据库详细设计文档-v1.0.md`
- `项目开发分工与双数据库设计-v1.0.md`
- 当前已生成前端代码（React + TypeScript + Zustand + React Query）

目标是给出一份面向第一版落地的后端技术方案，明确：

- 第一版真实开发范围
- TS 后端整体架构
- 业务服务与积分服务边界
- 双库一致性与异步任务方案
- 与当前前端代码的对接方式
- 模块拆分、开发顺序、风险与待确认项

## 2. 产品与范围分析

### 2.1 目标用户

- 服装电商商家
- 品牌内容团队
- 电商视觉与运营团队
- 对低成本批量生成商品内容有强需求的企业客户

### 2.2 核心业务目标

- 用 AI 替代部分传统服装拍摄与修图流程
- 形成“上传素材 -> 生成任务 -> 结果下载 -> 项目沉淀 -> 积分扣费”的业务闭环
- 通过套餐、积分充值、企业版咨询形成商业闭环

### 2.3 第一版范围判断

结合《项目开发分工与双数据库设计-v1.0.md》，第一版应以“核心生成闭环 + 商业闭环”为主，不应把 PRD 中所有能力一次性做满。

第一版必须落地：

- 登录注册
- 首页公共内容接口
- AI 在线生成
  - 衣服图
  - 真人图
  - 人台图
  - AI 模特替换
- 视频生成 Beta
- 创意生图 Beta
- 模特、场景、模板配置查询
- 文件上传与素材管理
- 任务创建、查询、取消、重试、下载
- 我的项目
- 积分余额、流水、导出
- 套餐、充值、支付
- 企业申请与成片交付线索收集

第一版明确不做：

- AI 编辑工具箱 2.0 整体能力
- 企业 API 文档中心 2.0
- 私有化部署工作台 2.0
- 企业定制模型服务 2.0
- 复杂断点续传的完整交付

### 2.4 MVP 结论

从业务价值和开发成本看，第一版后端的最小闭环不是“全量 AI 平台”，而是：

1. 用户能注册登录并获得初始积分。
2. 用户能上传服装素材。
3. 用户能发起生成任务并看到进度。
4. 后端能可靠冻结积分、回调更新任务、成功结算、失败退款。
5. 用户能下载结果，并在项目、素材、积分页面查看历史记录。
6. 用户能购买套餐或充值积分。

这意味着后端设计必须优先保证账务一致性和任务可靠性，而不是先追求后台配置系统的完备度。

## 3. 前端现状分析

### 3.1 当前前端技术结构

现有前端项目不是空白模板，而是已具备明确模块边界的 React 工程：

- 框架：React 18 + TypeScript + Vite
- UI：Arco Design
- 数据请求：React Query
- 本地状态：Zustand
- HTTP 入口：`src/api/client.ts`
- 当前数据源：`src/services/mockService.ts`

### 3.2 当前已落地页面

根据路由与页面代码，当前前端已经覆盖：

- 首页 `/`
- 登录 `/login`
- 注册 `/register`
- 套餐 `/pricing`
- 企业版 `/enterprise`
- 成片交付 `/delivery`
- AI 生成工作台
  - `/ai-generate/clothes`
  - `/ai-generate/real`
  - `/ai-generate/mannequin`
  - `/ai-generate/model`
- AI 编辑 `/ai-edit`（前端占位，第一版后端不应实现 2.0 编辑链路）
- 视频 `/video`
- 创意生图 `/creative`
- 我的项目 `/projects`
- 素材管理 `/assets`
- 积分中心 `/credits`

### 3.3 当前前端真实依赖的后端能力

虽然当前页面仍在使用 mock 数据，但从页面结构可以明确看出真实后端接口需求已经成型：

- `AiGeneratePage`
  - 工作台模型列表
  - 场景列表
  - 最近结果
  - 积分余额与今日任务数
  - 上传文件
  - 获取计费预估
  - 创建任务
  - 任务进度订阅
- `ProjectsPage`
  - 项目列表
  - 项目详情
  - 项目下任务聚合
  - 批量下载入口
- `AssetsPage`
  - 素材列表
  - 文件夹管理
  - 上传、标签、收藏、分类
- `CreditsPage`
  - 余额
  - 冻结积分
  - 即将过期积分
  - 积分流水
  - 导出
- `LoginPage` / `RegisterPage`
  - 验证码发送
  - 手机号登录
  - 密码登录
  - 注册
  - Token 刷新

### 3.4 前后端对接判断

前端已经预留了真实 API Client，但目前有两个明显事实：

- 页面侧仍以内存状态和 mock 数据驱动，尚未进入真实任务状态机
- 前端本地 `useGenerateWorkbench` 的上传、生成、扣积分逻辑都是演示逻辑，未来必须由后端替换

因此后端方案不应围绕“如何配合 mock”，而应围绕“如何稳定接管页面中的关键状态来源”来设计：

- 计费结果以后端为准
- 任务状态以后端为准
- 积分余额以后端聚合返回为准
- 生成结果与下载以后端资产记录为准

## 4. 技术方案总览

### 4.1 语言与核心选型

后端开发语言采用 TypeScript，推荐如下技术栈：

- 运行时：Node.js 20 LTS
- Web 框架：NestJS
- ORM：Prisma
- 业务数据库：MySQL 8
- 积分数据库：MySQL 8 独立实例或独立库
- 缓存与分布式协调：Redis
- 队列：BullMQ
- 对象存储：S3 兼容对象存储 / 阿里云 OSS / 腾讯云 COS 任选一种抽象接入
- 实时推送：WebSocket，必要时可降级 SSE
- 鉴权：JWT Access Token + Refresh Token
- 文档：OpenAPI / Swagger
- 日志：Pino
- 校验：Zod 或 NestJS DTO + class-validator
- 任务调度：BullMQ + 定时任务

选择 NestJS 的原因：

- 适合模块化业务拆分
- 对控制器、服务、守卫、拦截器、定时任务、WebSocket 支持完整
- 便于统一接口规范、鉴权、审计和文档输出
- 对 TypeScript 团队长期维护更友好

### 4.2 服务形态建议

第一版建议采用“两服务 + 两数据库”的形态，而不是一开始拆成多微服务：

1. 业务后端 `biz-api`
2. 积分服务 `ledger-service`

理由：

- 文档已经明确业务服务与积分服务边界
- 账务逻辑需要独立治理
- 但第一版体量还不足以支撑更多微服务拆分
- 采用“少量独立服务 + 明确边界”比“单体大泥球”更适合后续扩展

### 4.3 高层架构

```text
Frontend Web
  -> Biz API (NestJS)
      -> Biz DB (MySQL)
      -> Redis
      -> BullMQ
      -> Object Storage / CDN
      -> AI Provider Adapters
      -> Ledger Service SDK / HTTP Client

Ledger Service (NestJS)
  -> Credit DB (MySQL)
  -> Redis
  -> BullMQ / Cron
```

### 4.4 核心设计原则

- 前端只访问业务后端，不直接访问积分服务
- 业务后端负责权限、资源归属、价格计算、任务编排和聚合查询
- 积分服务只负责账户、冻结、结算、退款、流水和对账
- 两库之间不做跨库事务，不使用跨库外键
- 依赖本地事务 + Outbox + 重试 + 幂等键保证最终一致性

## 5. 服务边界设计

### 5.1 业务后端职责

- 认证与会话
- 用户、企业、权限
- 模特、场景、模板
- 文件上传与素材管理
- 生成任务与 AI 供应商适配
- 项目与下载包
- 套餐、订阅、充值、支付
- 企业申请、销售线索、成片交付需求
- 聚合积分余额与流水给前端展示

### 5.2 积分服务职责

- 创建积分账户
- 入账
- 冻结积分
- 成功结算
- 失败释放/退款
- 流水查询与导出
- 幂等控制
- 余额并发控制
- 日快照与对账异常记录

### 5.3 不应混入积分服务的数据

- 用户手机号、昵称、企业资料
- 套餐定义与支付页面信息
- 任务详细参数与素材 URL
- AI 供应商调用细节

这部分边界必须严格执行，否则后续会出现：

- 账务库耦合业务含义
- 数据变更链路过长
- 查询和合规边界混乱

## 6. 后端模块拆分方案

### 6.1 业务后端模块

建议按领域拆分 NestJS 模块：

- `auth`
- `users`
- `enterprises`
- `rbac`
- `public-content`
- `assets`
- `folders`
- `catalog`
  - models
  - scenes
  - templates
- `projects`
- `tasks`
- `downloads`
- `pricing`
- `subscriptions`
- `recharge`
- `payments`
- `credits-gateway`
- `delivery`
- `enterprise-leads`
- `notifications`
- `realtime`
- `audit`
- `outbox`
- `scheduler`

### 6.2 积分服务模块

- `accounts`
- `grants`
- `billing-tasks`
- `transactions`
- `idempotency`
- `reconciliation`
- `ledger-outbox`
- `scheduler`
- `admin-adjustment`

### 6.3 基础设施层模块

两边都应抽象公共基础能力：

- `config`
- `database`
- `redis`
- `queue`
- `logger`
- `security`
- `errors`
- `common-response`

## 7. 数据库与双库方案

### 7.1 业务数据库定位

业务库是业务事实源，保存：

- 用户与认证
- 企业与成员
- 套餐、计价规则、订阅、支付
- 模特、场景、模板
- 素材、文件夹
- 项目与生成任务
- 下载包、交付、通知
- Outbox 与审计日志

### 7.2 积分数据库定位

积分库是账务事实源，保存：

- 账户
- 积分批次
- 账务任务
- 冻结明细
- 不可变流水
- 幂等键
- 账务 Outbox
- 日快照
- 对账异常

### 7.3 关键数据映射

业务库中所有涉及积分的实体都只存“外部引用”：

- `users.credit_account_id`
- `enterprises.credit_account_id`
- `generation_tasks.billing_task_id`
- `payment_orders.credit_account_id`

这符合文档中的双库映射规则，也便于后续分库扩容。

### 7.4 数据一致性策略

核心原则：

- 不做分布式事务
- 业务操作在业务库本地提交
- 跨库调用必须带 `idempotency_key`
- 所有关键跨库操作都写 Outbox
- 由补偿任务做最终一致性修复

适用链路：

- 注册赠送积分
- 创建生成任务并冻结积分
- 任务成功结算
- 任务失败退款
- 支付成功入账

## 8. 任务域技术方案

### 8.1 任务类型

按接口文档统一收口为：

- `clothes`
- `real`
- `mannequin`
- `model_replace`
- `video`
- `creative`

第一版数据库和接口都不应保留 `edit` 类型，以避免和“不做 AI 编辑 2.0”冲突。

### 8.2 任务状态模型

业务状态与账务状态分离保存。

业务状态：

```text
created
billing_pending
queued
processing
completed
failed
cancelling
cancelled
```

账务状态：

```text
not_started
freezing
frozen
settling
settled
refunding
refunded
failed
```

这是第一版能否稳定运行的关键。如果把两套状态合并成一个字段，后续补偿和对账会非常困难。

### 8.3 任务创建链路

推荐执行顺序：

1. 校验用户、权限、套餐并发额度。
2. 校验输入素材归属和可用状态。
3. 后端计算报价并验证 `quote_id` 未过期。
4. 在业务库创建 `generation_task`，状态为 `billing_pending`。
5. 调用积分服务冻结积分，幂等键为 `freeze:{task_id}`。
6. 冻结成功后更新 `billing_status=frozen`，并将任务投入队列。
7. Worker 调用 AI 供应商提交任务。
8. 供应商回调或轮询更新进度。
9. 成功则保存输出素材并结算积分。
10. 失败或取消则走退款/释放。

### 8.4 队列设计

建议至少拆成以下队列：

- `task-submit`
- `task-status-sync`
- `task-timeout-check`
- `download-package-build`
- `notification-send`
- `outbox-publish`
- `reconciliation-fix`

原因：

- AI 提交与状态同步是不同节奏
- 下载打包是独立耗时任务
- 对账补偿不应阻塞主交易链路

### 8.5 AI 供应商适配层

必须做统一适配，不允许业务服务直接耦合某一家 AI 平台 SDK。

统一接口建议：

- `estimateCapability()`
- `submitTask()`
- `getTaskStatus()`
- `cancelTask()`
- `verifyCallback()`
- `normalizeResult()`
- `normalizeError()`

每个供应商单独实现 adapter，例如：

- `provider-a-image.adapter.ts`
- `provider-b-video.adapter.ts`

业务层只依赖统一契约，不依赖供应商差异。

### 8.6 实时进度推送

第一版建议使用 WebSocket。

原因：

- 当前前端工作台是任务驱动界面，不只是表单提交
- 任务状态、阶段、进度、结果图、退款状态都需要主动推送
- WebSocket 比轮询更适合生成工作台

推送事件建议与接口文档一致：

- `task.created`
- `task.updated`
- `task.completed`
- `task.failed`
- `task.refunded`

前端连接策略：

- 登录后获取短期 `ws_token`
- 基于用户维度订阅 `user:{userId}:tasks`
- 项目页和工作台复用同一通道

## 9. 文件与素材方案

### 9.1 上传模式

第一版建议采用“两阶段上传”：

1. 前端向业务后端申请上传
2. 后端返回对象存储上传参数
3. 前端直传对象存储
4. 前端回调业务后端确认完成
5. 后端做异步元数据提取与审核

这样比“文件经过业务服务中转”更适合图片类场景，减轻业务服务带宽压力。

### 9.2 第一版必须支持的能力

- 单文件上传
- 批量上传
- 秒传检查
- MIME / 尺寸 / 分辨率校验
- 元数据提取
- 缩略图
- 资源归属绑定
- 素材标签、收藏、文件夹

### 9.3 第一版可预留不完全实现的能力

- 断点续传会话接口预留
- 但不要求第一版完成复杂大文件分片协议

### 9.4 素材生命周期

建议分成以下状态：

- `uploading`
- `ready`
- `failed`
- `deleted`

审核状态单独保存：

- `pending`
- `passed`
- `rejected`

这样可以避免把“上传失败”和“审核失败”混成一类。

## 10. 认证与权限方案

### 10.1 认证方式

第一版建议支持：

- 手机验证码登录
- 手机号 + 密码登录
- 注册
- Token 刷新
- 退出登录
- 重置密码

微信扫码登录可以先预留接口协议和表结构，但是否首版真正接通，取决于渠道准备情况。

### 10.2 Token 策略

- Access Token：短期 JWT，例如 30 分钟
- Refresh Token：长效，例如 30 天
- Refresh Token 仅存 hash
- 支持多设备会话记录

### 10.3 权限模型

建议第一版采用轻 RBAC：

- 普通用户
- 企业管理员
- 企业成员
- 管理后台运营角色

接口层面按资源和动作控制：

- `task:create`
- `task:read`
- `task:cancel`
- `asset:read`
- `asset:write`
- `project:read`
- `credit:read`
- `payment:create`

### 10.4 资源归属校验

所有业务查询必须附带归属条件：

- `user_id`
- `owner_type`
- `owner_id`
- `enterprise_id`

不能只靠前端隐藏按钮，也不能只靠 URL 中的 ID。

## 11. 套餐、计价与支付方案

### 11.1 计价原则

文档已经明确：计费预估只能由后端计算。

前端提交的参数只作为输入，不作为扣费依据。实际价格取决于：

- 当前套餐
- 任务类型
- 输出数量
- 分辨率或模板参数
- 有效中的计价规则版本

### 11.2 报价对象

建议保留 `task_quotes` 设计，作为创建任务前的短期报价快照：

- 有效期默认 5 分钟
- 使用后变为 `used`
- 超时后自动 `expired`

好处：

- 保证任务创建时价格和前端确认时一致
- 保留定价快照，便于事后审计

### 11.3 支付模型

业务库负责：

- 套餐定义
- 充值商品
- 订单创建
- 渠道支付回调
- 订单状态变更

积分服务负责：

- 支付成功后入账
- 保存充值批次与交易流水

### 11.4 支付回调

支付回调必须满足：

- 验签
- 幂等
- 重复通知安全处理
- 入账失败可补偿

推荐流程：

1. 接收回调
2. 验签并落库原始回调
3. 更新订单为已支付
4. 写业务 Outbox
5. 异步调用积分服务入账
6. 对账任务兜底修复未入账订单

## 12. 与前端的接口对接方案

### 12.1 对接原则

当前前端的 mockService 应逐步替换为真实 API 模块，不建议一次性整体替换。

建议按业务域对接：

- `authApi`
- `publicApi`
- `catalogApi`
- `assetApi`
- `taskApi`
- `projectApi`
- `creditApi`
- `pricingApi`
- `paymentApi`

### 12.2 第一批必须优先打通的接口

优先级 P0：

- 认证接口
- 当前用户接口
- 首页公共配置接口
- 模特/场景接口
- 上传秒传检查
- 上传完成确认
- 任务预估
- 创建任务
- 任务列表/详情
- 实时推送 token
- 积分余额

优先级 P1：

- 素材管理
- 项目列表与详情
- 下载包
- 流水与导出
- 套餐列表
- 创建支付订单

优先级 P2：

- 企业申请
- 成片交付
- 视频与创意模板扩展

### 12.3 前端需要被后端接管的本地状态

当前前端中以下本地逻辑必须逐步移除或降级为 UI 临时态：

- `useGenerateWorkbench` 中的本地伪上传进度
- 本地随机生成结果图
- 本地扣减积分
- 本地任务完成状态

保留在前端的应是：

- 选择中的模型、场景、参数
- 本地表单校验
- 上传过程中的临时展示态

## 13. 工程与目录建议

### 13.1 业务后端目录建议

```text
apps/
  biz-api/
    src/
      modules/
      infra/
      common/
      main.ts
```

模块内部建议统一结构：

```text
tasks/
  controllers/
  services/
  repositories/
  dto/
  entities/
  mappers/
  events/
  workers/
```

### 13.2 积分服务目录建议

```text
apps/
  ledger-service/
    src/
      modules/
      infra/
      common/
      main.ts
```

### 13.3 共用包建议

如果采用 monorepo，可以抽公共包：

- `packages/shared-types`
- `packages/shared-kernel`
- `packages/shared-errors`
- `packages/shared-utils`

但第一版不建议过度抽象，避免在业务尚未稳定前过早设计通用层。

## 14. 关键非功能方案

### 14.1 安全

- 密码使用 Argon2id
- 手机号、邮箱落库加密，查询使用 hash
- 上传文件校验真实 MIME
- 管理和账务操作写审计日志
- 结果下载使用短期签名 URL
- 支付和积分流水不可物理修改

### 14.2 性能

- 列表接口默认分页
- 模特、场景、套餐等基础配置做短期缓存
- 最近任务、余额概览可做用户级缓存
- 大图下载、ZIP 打包走异步

### 14.3 稳定性

- 关键写接口必须幂等
- 回调处理必须幂等
- 队列任务必须可重试
- 定时补偿覆盖超时、漏回调、漏入账、漏退款

### 14.4 可观测性

- 每个请求带 `request_id`
- 结构化日志
- 任务链路日志
- 支付链路日志
- 积分链路日志
- 队列积压告警
- 对账异常告警

## 15. 开发阶段建议

### 阶段一：基础框架

- NestJS 双服务骨架
- MySQL 双库
- Prisma schema
- Redis 与 BullMQ
- 统一响应、错误码、鉴权、日志
- 用户、认证、积分账户创建

### 阶段二：核心图片生成闭环

- 模特/场景查询
- 上传与素材
- 任务报价
- 任务创建
- 积分冻结
- AI 供应商接入
- 回调与进度同步
- 成功结算 / 失败退款
- 工作台实时推送

### 阶段三：用户中心与商业闭环

- 项目列表与详情
- 素材管理增强
- 积分流水与导出
- 套餐、充值、支付
- 支付成功入账

### 阶段四：扩展能力

- 视频生成 Beta
- 创意生图 Beta
- 企业申请
- 成片交付需求

### 阶段五：上线前稳定化

- 对账
- 补偿
- 限流
- 压测
- 监控告警
- 安全审计

## 16. 主要风险与解决建议

### 16.1 最大风险：任务与积分不一致

风险表现：

- 任务成功但未扣费
- 任务失败但未退款
- 重复回调导致重复结算

解决建议：

- 冻结、结算、退款都必须幂等
- 业务状态与账务状态分离
- Outbox + 定时对账补偿

### 16.2 第二风险：AI 供应商不稳定

风险表现：

- 回调延迟
- 状态不一致
- 供应商接口变更

解决建议：

- 统一适配层
- 回调 + 轮询双保险
- provider 记录与 attempt 记录完整留痕

### 16.3 第三风险：前后端状态模型不一致

风险表现：

- 前端仍按 mock 心智处理任务
- 真实状态机接入后页面逻辑冲突

解决建议：

- 先定义统一 DTO
- 先接“只读真实接口”，再接“写操作”
- 用任务事件驱动页面刷新

## 17. 结论

这套平台的后端第一版，技术重点不是“普通内容管理后台”，而是“任务编排 + 账务一致性 + 文件资产 + 实时状态”的组合系统。

如果用 TypeScript 落地，最合适的实现方式是：

- 用 NestJS 构建业务后端和积分服务
- 用 MySQL 双库承载业务事实与账务事实
- 用 Redis + BullMQ 承载异步任务、补偿与实时协作
- 用统一 AI 适配层屏蔽外部供应商差异
- 用 Outbox + 幂等 + 对账机制保证跨库最终一致性

从当前前端代码看，页面结构和数据边界已经足够明确，后端完全可以按本文方案进入接口建模和工程落地阶段。下一步最合理的动作不是直接写业务代码，而是先输出：

1. API DTO 清单
2. 模块依赖图
3. 数据库迁移顺序
4. 任务时序图
5. 前后端联调优先级表

