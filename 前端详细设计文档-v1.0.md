# 服装电商内容生成平台前端详细设计文档 v1.0

## 1. 文档目标

本文定义第一版 Web 前端的页面、组件、状态、交互、接口依赖和验收要求，供 UI、前端、后端和测试共同评审。

第一版不开发任何标记为 `2.0` 的功能：

- AI 编辑工具箱及其六类编辑能力。
- 企业 API 文档和 SDK 示例。
- 私有化部署工作台。
- 企业定制模特工作台。

第一版任务类型仅允许：

```text
clothes
real
mannequin
model_replace
video
creative
```

## 2. 前端职责边界

前端负责：

- 页面展示、表单交互、客户端基础校验和用户操作反馈。
- 登录态维护、路由权限和按钮权限。
- 文件上传队列、任务状态订阅、结果预览和下载。
- 展示后端返回的积分预估、余额、扣费和退款状态。
- 页面埋点、错误上报和性能监控。

前端不负责：

- 最终积分价格计算。
- 判断正式余额是否充足。
- 修改任务状态或账务状态。
- 直接访问积分服务、数据库、对象存储管理接口或 AI 供应商。
- 在客户端保存支付密钥、AI 密钥和长期有效下载地址。

## 3. 建议技术结构

具体框架可在项目启动时确定，建议模块边界保持如下：

```text
src/
  api/             统一 API Client 与接口模块
  assets/          静态资源
  components/      通用组件
  composables/     上传、任务订阅、权限等复用逻辑
  layouts/         公共布局
  pages/           路由页面
  router/          路由和守卫
  stores/          用户、任务、积分、上传状态
  styles/          Token、主题和全局样式
  types/           API DTO 与前端类型
  utils/           格式化、校验、下载等工具
```

建议状态域：

- `authStore`：用户、Token 状态、企业和权限。
- `creditStore`：余额、冻结、即将过期积分和刷新时间。
- `taskStore`：进行中的任务、订阅状态和通知。
- `uploadStore`：上传队列、进度、失败和重试。
- `appStore`：全局配置、维护状态和公共字典。

## 4. 路由与权限

|路由|页面|登录要求|主要权限|
|---|---|---|---|
|`/`|首页|否|公开|
|`/generate/clothes`|衣服图生成|是|`task:create`|
|`/generate/real`|真人图生成|是|`task:create`|
|`/generate/mannequin`|人台图生成|是|`task:create`|
|`/generate/model-replace`|模特替换|是|`task:create`|
|`/video`|视频生成|是|`task:create_video`|
|`/creative`|创意生图|是|`task:create_creative`|
|`/delivery`|成片交付|否|提交时要求登录|
|`/enterprise`|企业版介绍|否|公开|
|`/projects`|我的项目|是|`project:read`|
|`/projects/:id`|项目详情|是|资源归属校验|
|`/tasks/:id`|任务详情|是|资源归属校验|
|`/assets`|素材管理|是|`asset:read`|
|`/credits`|积分中心|是|`credit:read`|
|`/pricing`|套餐与充值|否|购买时要求登录|
|`/orders/:id`|支付结果|是|资源归属校验|
|`/account`|账号设置|是|本人|
|`/enterprise/apply`|企业申请|是|本人|

路由守卫：

1. 未登录访问受限路由时打开登录弹窗，登录成功后返回原路由。
2. 无权限时进入 `403` 页面，不只隐藏按钮。
3. Token 过期时先静默刷新；刷新失败后清理登录态并打开登录弹窗。
4. 系统维护时只允许访问首页、登录、维护说明和支付回调结果页。

## 5. 设计系统与通用组件

### 5.1 基础视觉

- 定义品牌主色、成功色、警告色、错误色、中性色。
- 定义 `4/8/12/16/24/32/48px` 间距阶梯。
- 定义标题、正文、说明、数字金额等文字层级。
- PC 优先，最低支持 `1280px` 工作台宽度；营销页兼容平板和手机。
- 图片区域统一使用固定比例容器，避免加载时页面抖动。

### 5.2 通用组件

- `AppHeader`：导航、积分余额、用户菜单。
- `AuthModal`：登录、注册、忘记密码、微信登录。
- `UploadZone`：拖拽、选择、校验和上传队列。
- `AssetPicker`：从素材库选择文件。
- `ModelPicker`：模特筛选、详情和收藏。
- `ScenePicker`：场景筛选、详情和收藏。
- `TaskConfigPanel`：任务类型对应参数。
- `CreditEstimateCard`：预估积分、余额和不足提示。
- `TaskProgressCard`：排队、进度、阶段、取消和失败。
- `ResultViewer`：图片/视频预览、放大、下载和对比。
- `FilterBar`：关键词、类型、状态和时间。
- `EmptyState`、`ErrorState`、`PermissionState`。
- `PaymentModal`：支付方式、二维码和支付状态。
- `ConfirmDialog`：取消任务、删除素材、退出登录等危险操作。

### 5.3 统一状态

|状态|展示要求|
|---|---|
|首次加载|页面级 Skeleton|
|局部加载|区域 Spinner，不遮挡其他可操作区域|
|提交中|按钮禁用并显示“处理中...”|
|空数据|说明原因并提供主要操作|
|网络失败|保留用户输入并提供重试|
|权限不足|说明缺少的权限或套餐要求|
|系统错误|展示可读文案和 `request_id`|
|长任务|显示当前阶段、进度、创建时间和预计时间|

## 6. 公共页面详细设计

### 6.1 首页

页面模块：

1. 顶部导航和登录入口。
2. Hero 文案、立即生成、立即咨询。
3. 能力入口：衣服图、真人图、人台图、模特替换、视频、创意生图。
4. 生成案例和前后对比。
5. 数据背书、客户 Logo 和交付流程。
6. 套餐摘要。
7. 企业版介绍和销售咨询。
8. 页脚、协议、隐私和联系方式。

交互：

- 未登录点击生成入口，记录目标路由后打开登录弹窗。
- Beta 功能显示 `Beta` 标签，但不出现 2.0 功能入口。
- 案例图片懒加载，失败时使用占位图。
- 咨询表单提交成功后显示受理编号。

接口：

- `GET /api/v1/public/home-config`
- `GET /api/v1/public/cases`
- `POST /api/v1/leads`

### 6.2 登录注册

支持模式：

- 手机号 + 验证码登录。
- 手机号 + 密码登录。
- 手机号注册。
- 忘记密码。
- 微信扫码登录。
- 企业账号登录。

字段规则：

|字段|前端规则|
|---|---|
|手机号|中国大陆 11 位，输入时过滤非数字|
|验证码|6 位数字，发送后倒计时 60 秒|
|密码|8-20 位，至少包含字母和数字|
|确认密码|必须和密码一致|
|协议|注册前必须勾选|

状态处理：

- 验证码倒计时以服务端返回时间为准，本地只负责展示。
- 连续错误触发图形验证码时，打开验证码组件后重新发送。
- 微信二维码每 2 秒轮询，最长 60 秒；成功后停止轮询。
- 注册成功后等待后端完成积分账户初始化，再进入工作台。
- 后端返回“账户初始化中”时展示短暂等待页并轮询用户初始化状态。

接口：

- `POST /api/v1/auth/sms/send`
- `POST /api/v1/auth/register`
- `POST /api/v1/auth/login`
- `POST /api/v1/auth/token/refresh`
- `POST /api/v1/auth/logout`
- `POST /api/v1/auth/password/reset`
- `POST /api/v1/auth/wechat/qrcode`
- `GET /api/v1/auth/wechat/status?state={state}`
- `GET /api/v1/users/me`

### 6.3 企业版与成片交付

企业版页面：

- 版本权益对比。
- 企业能力说明。
- 合作平台展示。
- 企业申请和销售咨询。
- 2.0 能力只可作为“规划中”文字，不提供可点击入口和承诺上线日期。

成片交付表单：

|字段|规则|
|---|---|
|需求标题|2-100 字|
|需求描述|10-2000 字|
|用途场景|枚举 + 其他|
|期望风格|1-10 个标签|
|交付时间|不得早于当前日期|
|企业名称|2-100 字|
|联系人|2-30 字|
|手机号|11 位|
|邮箱|可选，标准邮箱格式|
|附件|最多 20 个，单个不超过 50MB|

接口：

- `POST /api/v1/enterprise/applications`
- `GET /api/v1/enterprise/application`
- `POST /api/v1/delivery-requests`
- `GET /api/v1/delivery-requests`
- `GET /api/v1/delivery-requests/{id}`

## 7. AI 生成工作台

### 7.1 统一布局

建议三栏结构：

- 左栏：任务类型、上传图片和历史素材。
- 中栏：模特、场景、模板和参数选择。
- 右栏：积分预估、生成按钮、任务状态和结果。

工作台状态：

```text
empty -> uploading -> configuring -> estimating
      -> submitting -> queued -> processing
      -> completed | failed | cancelled | refunded
```

生成按钮启用条件：

- 用户已登录且拥有权限。
- 上传素材全部完成且审核通过。
- 必填参数齐全。
- 积分预估已获取且未过期。
- 后端返回余额充足。
- 当前并发未超过套餐限制。
- 当前没有重复提交请求。

### 7.2 任务预估

任何影响价格的参数变化后，前端以 300ms 防抖调用：

- `POST /api/v1/tasks/estimate`

请求至少包含：

- `type`
- `input_asset_ids`
- `model_id`
- `scene_id`
- `config`

展示：

- 单价、数量、预估总积分。
- 当前可用积分。
- 任务提交后预计剩余积分。
- 价格快照有效期。

提交任务时必须携带后端返回的 `quote_id`，后端重新校验价格；前端不可只提交自行计算的数字。

创建接口：

- `POST /api/v1/tasks`

### 7.3 衣服图

必填：

- 1-10 张服装图。
- 模特。
- 场景。
- 输出比例。
- 输出数量。

可选：

- 姿态。
- 锁定模特。
- 提示词补充。

校验：

- JPG/PNG，单张不超过 20MB。
- 最小边不低于 500px，建议不低于 1024px。
- 批量任务所有文件审核通过后才允许整体提交。

### 7.4 真人图

必填：

- 1 张真人着装图。
- 处理模式：换模特、换场景、模特和场景都换。

按模式动态要求：

- 换模特时必须选择目标模特。
- 换场景时必须选择目标场景。

### 7.5 人台图

必填：

- 1 张人台图。
- 目标模特。
- 场景。

提交前提示：

- 图片应清晰显示服装主体。
- 避免遮挡、过曝和复杂背景。

### 7.6 模特替换

必填：

- 1 张真人着装图。
- 目标模特。

可选：

- 保留背景。
- 选择新场景。
- 锁定服装细节强度。

### 7.7 视频生成

必填：

- 1 张人物着装图。
- 动作模板。
- 视频风格。
- 时长：5/10/15/30 秒。
- 分辨率：720P/1080P。

前端必须明确：

- Beta 标签。
- 预估生成时间。
- 不同时长和分辨率对应的积分变化。

### 7.8 创意生图

必填：

- 1 张商品或人物图。
- 创意模板。
- 输出数量。

可选：

- 品牌调性。
- 创意强度。
- 风格关键词。
- 输出比例。

### 7.9 任务状态与推送

首选 WebSocket：

- `GET /api/v1/ws?token={short_lived_ws_token}`

事件：

```json
{
  "event": "task.updated",
  "data": {
    "task_id": "tsk_xxx",
    "status": "processing",
    "progress": 52,
    "stage": "generating",
    "queue_position": null,
    "billing_status": "frozen",
    "updated_at": "2026-06-09T12:00:00+08:00"
  }
}
```

降级策略：

- WebSocket 连接失败后，每 3 秒调用 `GET /api/v1/tasks/{id}`。
- 页面进入后台后降低到 10 秒。
- 任务终态后停止轮询。
- 页面重新激活时立即拉取一次最新状态。

任务状态展示：

|后端状态|前端文案|允许操作|
|---|---|---|
|`billing_pending`|正在确认积分|等待|
|`queued`|排队中|未开始时可取消|
|`processing`|生成中|按后端 `cancellable` 决定|
|`completed`|已完成|预览、下载、重新生成|
|`failed`|生成失败|查看原因、重新生成|
|`cancelling`|取消处理中|等待|
|`cancelled`|已取消|重新生成|
|`refunding`|退款处理中|查看积分记录|
|`refunded`|已退款|重新生成|

## 8. 我的项目与任务详情

### 8.1 项目列表

列表字段：

- 封面。
- 项目名称。
- 任务数量。
- 最近任务状态。
- 更新时间。
- 创建人。

操作：

- 新建、重命名、删除。
- 查看详情。
- 批量下载项目结果。

接口：

- `GET /api/v1/projects`
- `POST /api/v1/projects`
- `GET /api/v1/projects/{id}`
- `PATCH /api/v1/projects/{id}`
- `DELETE /api/v1/projects/{id}`
- `POST /api/v1/projects/{id}/items`
- `DELETE /api/v1/projects/{id}/items/{task_id}`
- `POST /api/v1/projects/{id}/download`

### 8.2 任务列表

筛选：

- 类型。
- 状态。
- 创建时间。
- 关键词。
- 项目。

第一版类型筛选不包含 `edit`。

接口：

- `GET /api/v1/tasks`
- `GET /api/v1/tasks/{id}`
- `POST /api/v1/tasks`
- `POST /api/v1/tasks/{id}/cancel`
- `POST /api/v1/tasks/{id}/retry`
- `POST /api/v1/tasks/{id}/download`
- `DELETE /api/v1/tasks/{id}`

任务详情展示：

- 输入素材。
- 输出结果。
- 模特、场景、模板和完整参数。
- 创建、开始、完成时间。
- 任务状态和进度事件。
- 预估积分、实际结算积分和账务状态。
- 失败错误码、用户可读原因和建议操作。

## 9. 素材管理

页面能力：

- 网格/列表切换。
- 文件夹树。
- 类型、格式、时间和收藏筛选。
- 上传、重命名、移动、收藏、删除。
- 图片和视频详情。
- 模特/场景收藏页签。

接口：

- `POST /api/v1/uploads/check`
- `POST /api/v1/uploads`
- `POST /api/v1/uploads/batch`
- `GET /api/v1/assets`
- `GET /api/v1/assets/{id}`
- `PATCH /api/v1/assets/{id}`
- `DELETE /api/v1/assets/{id}`
- `POST /api/v1/assets/{id}/favorite`
- `DELETE /api/v1/assets/{id}/favorite`
- `GET /api/v1/asset-folders`
- `POST /api/v1/asset-folders`
- `PATCH /api/v1/asset-folders/{id}`
- `DELETE /api/v1/asset-folders/{id}`

删除规则：

- 被运行中任务引用的素材不可删除。
- 被历史任务引用时默认软删除，历史任务仍可查看。
- 删除前弹窗说明影响范围。

## 10. 模特、场景与模板

模特筛选：

- 人种、年龄、风格、身材、肤色、发色。
- 关键词。
- 热度、最新和收藏数排序。

场景筛选：

- 类型、色调、光线、关键词。

接口：

- `GET /api/v1/models`
- `GET /api/v1/models/{id}`
- `POST /api/v1/models/{id}/favorite`
- `DELETE /api/v1/models/{id}/favorite`
- `GET /api/v1/scenes`
- `GET /api/v1/scenes/{id}`
- `POST /api/v1/scenes/{id}/favorite`
- `DELETE /api/v1/scenes/{id}/favorite`
- `GET /api/v1/templates/actions`
- `GET /api/v1/templates/videos`
- `GET /api/v1/templates/creative`

## 11. 积分、套餐和支付

### 11.1 积分中心

余额卡：

- 总余额。
- 可用余额。
- 冻结余额。
- 7 天内即将过期积分。
- 数据更新时间。

流水字段：

- 时间。
- 类型。
- 变动积分。
- 变动后余额。
- 状态。
- 关联业务。
- 描述。

接口：

- `GET /api/v1/credits/balance`
- `GET /api/v1/credits/transactions`
- `POST /api/v1/credits/transactions/export`

### 11.2 套餐

展示：

- 当前套餐。
- 起止时间。
- 图片任务并发。
- 批量任务并发。
- 视频权限。
- 每月赠送积分。
- 专属场景等权益。

接口：

- `GET /api/v1/plans`
- `GET /api/v1/subscriptions/current`
- `POST /api/v1/subscriptions/orders`

### 11.3 充值与支付

流程：

1. 选择充值商品或套餐。
2. 创建业务支付订单。
3. 选择微信/支付宝。
4. 展示支付二维码或跳转链接。
5. 每 2 秒查询支付状态。
6. 支付成功后刷新订单、订阅和积分余额。

接口：

- `GET /api/v1/recharge-products`
- `POST /api/v1/payment-orders`
- `GET /api/v1/payment-orders/{id}`
- `POST /api/v1/payment-orders/{id}/close`

支付状态：

- `pending`
- `paying`
- `paid`
- `crediting`
- `completed`
- `closed`
- `failed`
- `refunded`

支付成功但积分未到账时展示“支付成功，积分入账中”，不可误报支付失败。

## 12. 错误处理

统一错误结构：

```json
{
  "code": 300001,
  "message": "积分不足",
  "data": {
    "required": 40,
    "available": 25
  },
  "request_id": "req_xxx"
}
```

重点处理：

|错误码|前端处理|
|---|---|
|`200001`|手机号已注册，引导登录|
|`200002`|手机号未注册，引导注册|
|`200003`|清空验证码并聚焦|
|`200006`|展示锁定剩余时间|
|`300001`|展示缺口并跳转充值|
|`300002`|显示当前并发和上限|
|`300003`|保留配置，建议稍后重试|
|`400002`|刷新任务详情后更新按钮|
|`500001-500003`|定位到具体失败文件|
|`900001`|跳转维护页面|
|`900002`|保留输入并提供重试|

## 13. 安全要求

- Refresh Token 使用 `httpOnly` Cookie，不存入 Local Storage。
- Access Token 仅保存在内存；刷新页面后通过 Refresh Token 恢复。
- 所有用户输入按纯文本渲染。
- 下载链接只使用后端返回的短期签名地址。
- 图片 URL 不允许拼接任意域名，配置可信 CDN 域名白名单。
- 不在日志、埋点和错误上报中发送 Token、手机号全文、验证码和支付参数。
- 危险操作携带 CSRF Token 或使用严格 SameSite Cookie 策略。

## 14. 性能与体验指标

- 营销页首屏 LCP 目标小于 3 秒。
- 工作台路由切换后 1 秒内显示可操作骨架。
- 长列表使用分页或虚拟列表。
- 图片使用缩略图，查看详情时再加载原图。
- 搜索防抖 300ms。
- 上传进度刷新节流 100ms。
- 重复点击创建任务只能产生一次有效请求。

## 15. 埋点与监控

核心事件：

- `landing_page_view`
- `auth_modal_open`
- `login_success`
- `register_success`
- `workspace_enter`
- `upload_start`
- `upload_success`
- `task_estimate`
- `generate_click`
- `generate_success`
- `generate_fail`
- `task_cancel`
- `result_download`
- `pricing_page_view`
- `payment_order_create`
- `payment_success`

所有任务事件携带：

- `task_type`
- `task_id`（创建后）
- `quote_id`
- `estimated_credits`
- `request_id`

不得携带图片 URL、手机号、提示词全文等敏感数据。

## 16. 前端测试与验收

单元测试：

- 字段校验。
- 错误码映射。
- 任务状态到按钮状态的映射。
- 金额和积分格式化。

组件测试：

- 登录弹窗。
- 上传队列。
- 积分预估卡。
- 任务进度卡。
- 支付弹窗。

端到端测试：

1. 注册、登录和 Token 刷新。
2. 上传图片、获取预估、创建任务。
3. 任务成功、结果预览和下载。
4. 任务失败、自动退款和流水可见。
5. 积分不足后充值并重新提交。
6. 并发超限。
7. WebSocket 断开后轮询恢复。
8. 支付成功但积分入账延迟。
9. 用户不能访问他人的项目、任务和素材。
10. 页面和接口中没有任何 `edit` 或 2.0 可操作入口。

## 17. 前端待确认项

- 新人福利最终是 10 张还是 40 张对应积分。
- 视频和创意生图是否第一版正式开放。
- 任务处理中取消的退款规则。
- 企业账号具体权限矩阵。
- 正式品牌文案、案例和统计数据。
- 支持的浏览器最低版本和移动端范围。
