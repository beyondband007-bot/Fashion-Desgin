# 服装电商内容生成平台数据库详细设计文档 v1.0

## 1. 设计目标

第一版使用两套独立 MySQL 数据库：

|数据库|建议库名|职责|
|---|---|---|
|业务数据库|`fashion_content_platform`|保存全部业务数据|
|积分数据库|`fashion_credit_ledger`|只保存积分账户和账务数据|

严格边界：

- 用户、企业、套餐、支付订单、素材、项目、任务、模特、场景都在业务库。
- 积分库只保存余额、积分批次、冻结、结算、退款、过期和流水。
- 两套库不建立跨库外键，不执行跨库 JOIN。
- 业务库只保存积分账户 ID、账务任务 ID 和计费结果快照。
- 积分库只保存 `owner_id/biz_id/source_order_id` 等外部引用，不复制业务详情。

## 2. 通用数据库规范

### 2.1 引擎与字符集

- MySQL 8.0+。
- 存储引擎：InnoDB。
- 字符集：`utf8mb4`。
- 排序规则：`utf8mb4_0900_ai_ci`，对大小写敏感的编码字段单独使用二进制排序。
- 时区：数据库统一保存 UTC，应用层转换为用户时区。

### 2.2 字段规范

|用途|建议类型|
|---|---|
|主键|`CHAR(26)`，ULID|
|外部平台数字 ID|`BIGINT` 或 `VARCHAR(128)`|
|积分|`DECIMAL(18,4)`|
|人民币金额|`DECIMAL(12,2)`|
|时间|`DATETIME(3)`|
|布尔值|`TINYINT(1)`|
|状态|`VARCHAR(32)`，应用层枚举校验|
|IP|`VARCHAR(45)`|
|URL|`VARCHAR(1024)`|
|JSON 配置|`JSON`|

公共字段按需使用：

- `created_at DATETIME(3) NOT NULL`
- `updated_at DATETIME(3) NOT NULL`
- `deleted_at DATETIME(3) NULL`
- `version INT NOT NULL DEFAULT 0`：乐观锁。

### 2.3 敏感数据

- 手机和邮箱使用 AES-256-GCM 加密后保存密文。
- 另存 SHA-256/HMAC 哈希用于唯一查询。
- 密码只保存 Argon2id/bcrypt 哈希。
- Refresh Token、验证码只保存哈希。
- 支付回调原文加密或脱敏保存。

## 3. 业务数据库

### 3.1 用户与认证

#### 3.1.1 `users`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|用户 ID|
|`phone_ciphertext`|VARBINARY(512)|NULL|手机号密文|
|`phone_hash`|CHAR(64)|UNIQUE, NULL|手机号查询哈希|
|`email_ciphertext`|VARBINARY(512)|NULL|邮箱密文|
|`email_hash`|CHAR(64)|UNIQUE, NULL|邮箱查询哈希|
|`password_hash`|VARCHAR(255)|NULL|密码哈希，微信用户可为空|
|`nickname`|VARCHAR(40)|NOT NULL|昵称|
|`avatar_asset_id`|CHAR(26)|NULL|头像素材|
|`user_type`|VARCHAR(20)|NOT NULL|`personal/enterprise`|
|`status`|VARCHAR(20)|NOT NULL|`active/frozen/deleted`|
|`register_source`|VARCHAR(20)|NOT NULL|`mobile/wechat/invite/admin`|
|`credit_account_id`|CHAR(26)|NULL, INDEX|积分库账户外部引用|
|`credit_account_status`|VARCHAR(20)|NOT NULL|`pending/ready/failed/disabled`|
|`nickname_updated_at`|DATETIME(3)|NULL|昵称上次修改时间|
|`last_login_at`|DATETIME(3)|NULL|最后登录时间|
|`last_login_ip`|VARCHAR(45)|NULL|最后登录 IP|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|
|`deleted_at`|DATETIME(3)|NULL|软删除时间|

索引：

- `uk_users_phone_hash(phone_hash)`
- `uk_users_email_hash(email_hash)`
- `idx_users_status_created(status, created_at)`
- `idx_users_credit_account(credit_account_id)`

#### 3.1.2 `user_oauth_bindings`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|绑定 ID|
|`user_id`|CHAR(26)|FK, INDEX|用户|
|`provider`|VARCHAR(20)|NOT NULL|`wechat`|
|`provider_openid`|VARCHAR(128)|NOT NULL|OpenID|
|`provider_unionid`|VARCHAR(128)|NULL|UnionID|
|`nickname_snapshot`|VARCHAR(100)|NULL|第三方昵称快照|
|`avatar_url_snapshot`|VARCHAR(1024)|NULL|头像快照|
|`access_scope`|VARCHAR(255)|NULL|授权范围|
|`bound_at`|DATETIME(3)|NOT NULL|绑定时间|
|`last_login_at`|DATETIME(3)|NULL|最后登录|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|

唯一键：

- `(provider, provider_openid)`
- `(provider, provider_unionid)`，UnionID 非空时唯一。

#### 3.1.3 `auth_sessions`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|会话 ID|
|`user_id`|CHAR(26)|FK, INDEX|用户|
|`refresh_token_hash`|CHAR(64)|UNIQUE|Refresh Token 哈希|
|`device_id`|VARCHAR(128)|NULL|设备标识|
|`device_name`|VARCHAR(100)|NULL|设备名称|
|`user_agent`|VARCHAR(512)|NULL|浏览器信息|
|`ip_address`|VARCHAR(45)|NULL|登录 IP|
|`status`|VARCHAR(20)|NOT NULL|`active/revoked/expired`|
|`expires_at`|DATETIME(3)|NOT NULL|过期时间|
|`last_active_at`|DATETIME(3)|NOT NULL|最后活跃|
|`revoked_at`|DATETIME(3)|NULL|撤销时间|
|`revoke_reason`|VARCHAR(64)|NULL|撤销原因|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|

#### 3.1.4 `verification_codes`

验证码实时校验建议使用 Redis，本表用于审计和频控留痕。

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|验证会话 ID|
|`target_hash`|CHAR(64)|INDEX|手机号/邮箱哈希|
|`purpose`|VARCHAR(32)|NOT NULL|用途|
|`code_hash`|CHAR(64)|NOT NULL|验证码哈希|
|`status`|VARCHAR(20)|NOT NULL|`sent/verified/expired/blocked`|
|`attempt_count`|INT|NOT NULL DEFAULT 0|错误次数|
|`send_channel`|VARCHAR(20)|NOT NULL|`sms/email`|
|`provider_message_id`|VARCHAR(128)|NULL|供应商消息 ID|
|`ip_address`|VARCHAR(45)|NULL|发送 IP|
|`expires_at`|DATETIME(3)|NOT NULL|过期时间|
|`verified_at`|DATETIME(3)|NULL|验证时间|
|`created_at`|DATETIME(3)|NOT NULL|发送时间|

#### 3.1.5 `password_history`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|记录 ID|
|`user_id`|CHAR(26)|FK, INDEX|用户|
|`password_hash`|VARCHAR(255)|NOT NULL|历史密码哈希|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|

每个用户只需保留最近 3-5 条。

### 3.2 企业与权限

#### 3.2.1 `enterprises`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|企业 ID|
|`code`|VARCHAR(32)|UNIQUE|企业登录代码|
|`name`|VARCHAR(160)|NOT NULL|企业名称|
|`industry`|VARCHAR(80)|NULL|行业|
|`employee_scale`|VARCHAR(32)|NULL|规模|
|`logo_asset_id`|CHAR(26)|NULL|企业 Logo|
|`status`|VARCHAR(20)|NOT NULL|`pending/active/suspended/closed`|
|`owner_user_id`|CHAR(26)|FK, INDEX|企业所有者|
|`credit_account_id`|CHAR(26)|NULL, INDEX|企业共享积分账户，可为空|
|`contact_name`|VARCHAR(60)|NULL|联系人|
|`contact_phone_ciphertext`|VARBINARY(512)|NULL|联系电话|
|`contact_email_ciphertext`|VARBINARY(512)|NULL|联系邮箱|
|`member_limit`|INT|NOT NULL DEFAULT 1|成员上限|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|

#### 3.2.2 `enterprise_members`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|成员关系 ID|
|`enterprise_id`|CHAR(26)|FK, INDEX|企业|
|`user_id`|CHAR(26)|FK, INDEX|用户|
|`role_code`|VARCHAR(32)|NOT NULL|`owner/admin/member`|
|`status`|VARCHAR(20)|NOT NULL|`invited/active/disabled/removed`|
|`invited_by`|CHAR(26)|NULL|邀请人|
|`joined_at`|DATETIME(3)|NULL|加入时间|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|

唯一键：`(enterprise_id, user_id)`。

#### 3.2.3 `enterprise_applications`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|申请 ID|
|`applicant_user_id`|CHAR(26)|FK, INDEX|申请人|
|`enterprise_name`|VARCHAR(160)|NOT NULL|企业名称|
|`contact_name`|VARCHAR(60)|NOT NULL|联系人|
|`contact_phone_ciphertext`|VARBINARY(512)|NOT NULL|电话|
|`contact_email_ciphertext`|VARBINARY(512)|NULL|邮箱|
|`industry`|VARCHAR(80)|NULL|行业|
|`employee_scale`|VARCHAR(32)|NULL|规模|
|`expected_usage`|TEXT|NOT NULL|使用需求|
|`business_license_asset_id`|CHAR(26)|NULL|营业执照|
|`status`|VARCHAR(20)|NOT NULL|申请状态|
|`reviewer_user_id`|CHAR(26)|NULL|审核人|
|`review_note`|VARCHAR(1000)|NULL|审核说明|
|`submitted_at`|DATETIME(3)|NULL|提交时间|
|`reviewed_at`|DATETIME(3)|NULL|审核时间|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|

### 3.3 套餐、计价、订阅与支付

#### 3.3.1 `plans`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|套餐 ID|
|`code`|VARCHAR(32)|UNIQUE|套餐编码|
|`name`|VARCHAR(80)|NOT NULL|名称|
|`description`|VARCHAR(500)|NULL|说明|
|`plan_type`|VARCHAR(20)|NOT NULL|`personal/enterprise`|
|`monthly_grant_credits`|DECIMAL(18,4)|NOT NULL DEFAULT 0|每周期赠送积分|
|`single_task_concurrency`|INT|NOT NULL|单任务并发|
|`batch_task_concurrency`|INT|NOT NULL|批量并发|
|`video_task_concurrency`|INT|NOT NULL DEFAULT 0|视频并发|
|`member_limit`|INT|NOT NULL DEFAULT 1|成员上限|
|`features_json`|JSON|NOT NULL|权益快照|
|`status`|VARCHAR(20)|NOT NULL|`draft/active/inactive`|
|`sort_order`|INT|NOT NULL DEFAULT 0|排序|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|

#### 3.3.2 `plan_prices`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|价格 ID|
|`plan_id`|CHAR(26)|FK, INDEX|套餐|
|`billing_cycle`|VARCHAR(20)|NOT NULL|`monthly/yearly`|
|`currency`|CHAR(3)|NOT NULL|`CNY`|
|`list_price`|DECIMAL(12,2)|NOT NULL|原价|
|`sale_price`|DECIMAL(12,2)|NOT NULL|售价|
|`effective_from`|DATETIME(3)|NOT NULL|生效时间|
|`effective_to`|DATETIME(3)|NULL|失效时间|
|`status`|VARCHAR(20)|NOT NULL|状态|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|

唯一键建议：`(plan_id, billing_cycle, effective_from)`。

#### 3.3.3 `credit_pricing_rules`

计费规则属于业务配置，保存在业务库。

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|规则 ID|
|`rule_version`|VARCHAR(32)|INDEX|规则版本|
|`plan_code`|VARCHAR(32)|INDEX|套餐编码|
|`task_type`|VARCHAR(32)|INDEX|任务类型|
|`unit`|VARCHAR(20)|NOT NULL|`image/video/task`|
|`unit_credits`|DECIMAL(18,4)|NOT NULL|基础单价|
|`conditions_json`|JSON|NULL|时长、分辨率等条件|
|`status`|VARCHAR(20)|NOT NULL|`draft/active/inactive`|
|`effective_from`|DATETIME(3)|NOT NULL|生效时间|
|`effective_to`|DATETIME(3)|NULL|结束时间|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|

#### 3.3.4 `user_subscriptions`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|订阅 ID|
|`owner_type`|VARCHAR(20)|NOT NULL|`user/enterprise`|
|`owner_id`|CHAR(26)|INDEX|用户或企业 ID|
|`plan_id`|CHAR(26)|FK, INDEX|套餐|
|`plan_snapshot_json`|JSON|NOT NULL|购买时权益快照|
|`billing_cycle`|VARCHAR(20)|NOT NULL|月/年|
|`status`|VARCHAR(20)|NOT NULL|`pending/active/expired/cancelled`|
|`start_at`|DATETIME(3)|NOT NULL|开始时间|
|`end_at`|DATETIME(3)|NOT NULL|结束时间|
|`auto_renew`|TINYINT(1)|NOT NULL DEFAULT 0|自动续费|
|`source_order_id`|CHAR(26)|NULL|支付订单|
|`credits_grant_status`|VARCHAR(20)|NOT NULL|积分赠送状态|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|

索引：`(owner_type, owner_id, status, end_at)`。

#### 3.3.5 `recharge_products`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|商品 ID|
|`code`|VARCHAR(32)|UNIQUE|商品编码|
|`name`|VARCHAR(100)|NOT NULL|商品名称|
|`price`|DECIMAL(12,2)|NOT NULL|人民币价格|
|`credits`|DECIMAL(18,4)|NOT NULL|基础积分|
|`bonus_credits`|DECIMAL(18,4)|NOT NULL DEFAULT 0|赠送积分|
|`expire_days`|INT|NULL|积分有效天数，空为永久|
|`status`|VARCHAR(20)|NOT NULL|`active/inactive`|
|`sort_order`|INT|NOT NULL DEFAULT 0|排序|
|`effective_from`|DATETIME(3)|NULL|生效时间|
|`effective_to`|DATETIME(3)|NULL|结束时间|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|

#### 3.3.6 `payment_orders`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|订单 ID|
|`order_no`|VARCHAR(64)|UNIQUE|业务订单号|
|`user_id`|CHAR(26)|FK, INDEX|下单用户|
|`enterprise_id`|CHAR(26)|NULL, INDEX|企业订单|
|`order_type`|VARCHAR(32)|NOT NULL|`credit_recharge/subscription`|
|`product_id`|CHAR(26)|NULL|充值商品|
|`plan_id`|CHAR(26)|NULL|套餐|
|`subject`|VARCHAR(200)|NOT NULL|订单标题|
|`amount`|DECIMAL(12,2)|NOT NULL|应付金额|
|`currency`|CHAR(3)|NOT NULL|币种|
|`payment_channel`|VARCHAR(20)|NOT NULL|微信/支付宝|
|`channel_trade_no`|VARCHAR(128)|NULL, INDEX|支付平台订单号|
|`status`|VARCHAR(20)|NOT NULL|订单状态|
|`credit_account_id`|CHAR(26)|NULL|目标积分账户|
|`credited_points`|DECIMAL(18,4)|NULL|已入账积分快照|
|`crediting_status`|VARCHAR(20)|NOT NULL|`not_started/pending/success/failed`|
|`idempotency_key`|VARCHAR(128)|NOT NULL|创建幂等键|
|`expire_at`|DATETIME(3)|NOT NULL|支付过期时间|
|`paid_at`|DATETIME(3)|NULL|支付时间|
|`completed_at`|DATETIME(3)|NULL|业务完成时间|
|`closed_at`|DATETIME(3)|NULL|关闭时间|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|

#### 3.3.7 `payment_callbacks`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|回调 ID|
|`payment_order_id`|CHAR(26)|NULL, INDEX|业务订单|
|`channel`|VARCHAR(20)|NOT NULL|渠道|
|`event_id`|VARCHAR(128)|NOT NULL|渠道事件 ID|
|`notify_id`|VARCHAR(128)|NULL|通知 ID|
|`signature_valid`|TINYINT(1)|NOT NULL|验签结果|
|`payload_encrypted`|LONGBLOB|NOT NULL|回调原文|
|`process_status`|VARCHAR(20)|NOT NULL|处理状态|
|`error_message`|VARCHAR(1000)|NULL|错误|
|`received_at`|DATETIME(3)|NOT NULL|接收时间|
|`processed_at`|DATETIME(3)|NULL|处理时间|

唯一键：`(channel, event_id)`。

### 3.4 模特、场景和模板

#### 3.4.1 `ai_models`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|模特 ID|
|`code`|VARCHAR(64)|UNIQUE|模特编码|
|`name`|VARCHAR(100)|NOT NULL|名称|
|`cover_asset_id`|CHAR(26)|NOT NULL|封面|
|`gallery_asset_ids`|JSON|NULL|多角度图片|
|`race`|VARCHAR(32)|NULL|人种|
|`age_group`|VARCHAR(32)|NULL|年龄段|
|`gender`|VARCHAR(20)|NULL|性别|
|`style`|VARCHAR(32)|NULL|风格|
|`body_type`|VARCHAR(32)|NULL|身材|
|`skin_tone`|VARCHAR(32)|NULL|肤色|
|`hair_color`|VARCHAR(32)|NULL|发色|
|`height_cm`|SMALLINT|NULL|身高|
|`measurements_json`|JSON|NULL|三围等|
|`provider_model_ref`|VARCHAR(128)|NULL|供应商模型引用|
|`status`|VARCHAR(20)|NOT NULL|`draft/active/inactive`|
|`sort_order`|INT|NOT NULL DEFAULT 0|排序|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|

#### 3.4.2 `scenes`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|场景 ID|
|`code`|VARCHAR(64)|UNIQUE|编码|
|`name`|VARCHAR(100)|NOT NULL|名称|
|`cover_asset_id`|CHAR(26)|NOT NULL|封面|
|`category`|VARCHAR(32)|NULL|类型|
|`tone`|VARCHAR(32)|NULL|色调|
|`lighting`|VARCHAR(32)|NULL|光线|
|`prompt_template`|TEXT|NULL|供应商提示模板|
|`negative_prompt`|TEXT|NULL|负面提示|
|`provider_scene_ref`|VARCHAR(128)|NULL|供应商引用|
|`status`|VARCHAR(20)|NOT NULL|状态|
|`sort_order`|INT|NOT NULL DEFAULT 0|排序|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|

#### 3.4.3 `generation_templates`

统一保存动作、视频和创意模板。

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|模板 ID|
|`template_type`|VARCHAR(20)|INDEX|`action/video/creative`|
|`code`|VARCHAR(64)|UNIQUE|编码|
|`name`|VARCHAR(100)|NOT NULL|名称|
|`description`|VARCHAR(500)|NULL|说明|
|`cover_asset_id`|CHAR(26)|NOT NULL|封面|
|`preview_asset_id`|CHAR(26)|NULL|视频/动图预览|
|`supported_task_types`|JSON|NOT NULL|支持任务类型|
|`config_schema_json`|JSON|NOT NULL|参数 Schema|
|`provider_config_json`|JSON|NULL|供应商参数模板|
|`status`|VARCHAR(20)|NOT NULL|状态|
|`sort_order`|INT|NOT NULL DEFAULT 0|排序|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|

#### 3.4.4 `user_model_favorites`

|字段|类型|约束|说明|
|---|---|---|---|
|`user_id`|CHAR(26)|PK 组成|用户|
|`model_id`|CHAR(26)|PK 组成|模特|
|`created_at`|DATETIME(3)|NOT NULL|收藏时间|

#### 3.4.5 `user_scene_favorites`

|字段|类型|约束|说明|
|---|---|---|---|
|`user_id`|CHAR(26)|PK 组成, FK|用户|
|`scene_id`|CHAR(26)|PK 组成, FK|场景|
|`created_at`|DATETIME(3)|NOT NULL|收藏时间|

### 3.5 素材与文件夹

#### 3.5.1 `asset_folders`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|文件夹 ID|
|`owner_type`|VARCHAR(20)|NOT NULL|`user/enterprise`|
|`owner_id`|CHAR(26)|INDEX|所有者|
|`parent_id`|CHAR(26)|NULL, INDEX|父文件夹|
|`name`|VARCHAR(100)|NOT NULL|名称|
|`path_cache`|VARCHAR(1000)|NULL|路径缓存|
|`sort_order`|INT|NOT NULL DEFAULT 0|排序|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|
|`deleted_at`|DATETIME(3)|NULL|软删除|

唯一键建议：`(owner_type, owner_id, parent_id, name, deleted_at)`。

#### 3.5.2 `assets`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|素材 ID|
|`owner_type`|VARCHAR(20)|NOT NULL|`user/enterprise/system`|
|`owner_id`|CHAR(26)|INDEX|所有者 ID|
|`folder_id`|CHAR(26)|NULL, INDEX|文件夹|
|`name`|VARCHAR(255)|NOT NULL|展示名称|
|`original_file_name`|VARCHAR(255)|NOT NULL|原文件名|
|`purpose`|VARCHAR(32)|INDEX|素材用途|
|`media_type`|VARCHAR(20)|NOT NULL|`image/video/file`|
|`mime_type`|VARCHAR(128)|NOT NULL|真实 MIME|
|`file_ext`|VARCHAR(16)|NOT NULL|扩展名|
|`file_size`|BIGINT UNSIGNED|NOT NULL|字节数|
|`sha256`|CHAR(64)|INDEX|文件摘要|
|`storage_provider`|VARCHAR(20)|NOT NULL|对象存储供应商|
|`storage_bucket`|VARCHAR(128)|NOT NULL|Bucket|
|`storage_key`|VARCHAR(1024)|NOT NULL|对象 Key|
|`public_url`|VARCHAR(1024)|NULL|公开/CDN 地址|
|`thumbnail_url`|VARCHAR(1024)|NULL|缩略图|
|`width`|INT|NULL|宽|
|`height`|INT|NULL|高|
|`duration_ms`|BIGINT|NULL|视频时长|
|`metadata_json`|JSON|NULL|EXIF 等|
|`review_status`|VARCHAR(20)|NOT NULL|`pending/passed/rejected`|
|`review_result_json`|JSON|NULL|审核结果|
|`favorite`|TINYINT(1)|NOT NULL DEFAULT 0|收藏|
|`status`|VARCHAR(20)|NOT NULL|`uploading/ready/failed/deleted`|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|
|`deleted_at`|DATETIME(3)|NULL|软删除|

索引：

- `(owner_type, owner_id, purpose, created_at)`
- `(owner_type, owner_id, folder_id, created_at)`
- `(owner_type, owner_id, sha256)`

### 3.6 项目与生成任务

#### 3.6.1 `projects`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|项目 ID|
|`owner_type`|VARCHAR(20)|NOT NULL|用户/企业|
|`owner_id`|CHAR(26)|INDEX|所有者|
|`created_by`|CHAR(26)|FK, INDEX|创建用户|
|`name`|VARCHAR(160)|NOT NULL|项目名称|
|`description`|VARCHAR(1000)|NULL|说明|
|`cover_asset_id`|CHAR(26)|NULL|封面|
|`status`|VARCHAR(20)|NOT NULL|`active/archived/deleted`|
|`task_count`|INT|NOT NULL DEFAULT 0|任务数缓存|
|`last_activity_at`|DATETIME(3)|NULL|最后活动|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|
|`deleted_at`|DATETIME(3)|NULL|软删除|

#### 3.6.2 `project_items`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|关联 ID|
|`project_id`|CHAR(26)|FK, INDEX|项目|
|`task_id`|CHAR(26)|FK, INDEX|任务|
|`sort_order`|INT|NOT NULL DEFAULT 0|排序|
|`added_by`|CHAR(26)|FK|操作人|
|`created_at`|DATETIME(3)|NOT NULL|加入时间|

唯一键：`(project_id, task_id)`。

#### 3.6.3 `task_quotes`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|报价 ID|
|`user_id`|CHAR(26)|FK, INDEX|用户|
|`billing_owner_type`|VARCHAR(20)|NOT NULL|用户/企业|
|`billing_owner_id`|CHAR(26)|NOT NULL|计费所有者|
|`credit_account_id`|CHAR(26)|NOT NULL|积分账户引用|
|`task_type`|VARCHAR(32)|NOT NULL|任务类型|
|`request_hash`|CHAR(64)|NOT NULL|参数摘要|
|`plan_code`|VARCHAR(32)|NOT NULL|套餐快照|
|`rule_version`|VARCHAR(32)|NOT NULL|计价版本|
|`unit_credits`|DECIMAL(18,4)|NOT NULL|单价|
|`billable_quantity`|DECIMAL(18,4)|NOT NULL|计费数量|
|`estimated_credits`|DECIMAL(18,4)|NOT NULL|预估积分|
|`config_snapshot_json`|JSON|NOT NULL|计价配置快照|
|`status`|VARCHAR(20)|NOT NULL|`active/used/expired`|
|`expires_at`|DATETIME(3)|NOT NULL|过期时间|
|`used_at`|DATETIME(3)|NULL|使用时间|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|

#### 3.6.4 `generation_tasks`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|任务 ID|
|`user_id`|CHAR(26)|FK, INDEX|创建用户|
|`owner_type`|VARCHAR(20)|NOT NULL|`user/enterprise`|
|`owner_id`|CHAR(26)|INDEX|资源所有者|
|`project_id`|CHAR(26)|NULL, INDEX|项目|
|`parent_task_id`|CHAR(26)|NULL, INDEX|重试来源|
|`quote_id`|CHAR(26)|FK|报价|
|`name`|VARCHAR(160)|NOT NULL|任务名称|
|`task_type`|VARCHAR(32)|INDEX|六类任务之一|
|`status`|VARCHAR(24)|INDEX|业务状态|
|`progress`|TINYINT UNSIGNED|NOT NULL DEFAULT 0|0-100|
|`stage`|VARCHAR(32)|NULL|处理阶段|
|`queue_priority`|INT|NOT NULL DEFAULT 0|队列优先级|
|`queue_position`|INT|NULL|排队位置快照|
|`model_id`|CHAR(26)|NULL|模特|
|`scene_id`|CHAR(26)|NULL|场景|
|`template_id`|CHAR(26)|NULL|模板|
|`config_json`|JSON|NOT NULL|完整参数|
|`config_version`|VARCHAR(32)|NOT NULL|参数版本|
|`billing_owner_type`|VARCHAR(20)|NOT NULL|计费归属|
|`billing_owner_id`|CHAR(26)|NOT NULL|计费归属 ID|
|`credit_account_id`|CHAR(26)|NOT NULL, INDEX|积分账户引用|
|`billing_task_id`|CHAR(26)|NULL, INDEX|积分库账务任务|
|`billing_status`|VARCHAR(24)|INDEX|账务状态|
|`estimated_credits`|DECIMAL(18,4)|NOT NULL|预估积分快照|
|`settled_credits`|DECIMAL(18,4)|NULL|结算积分快照|
|`error_code`|VARCHAR(64)|NULL|标准错误码|
|`error_message`|VARCHAR(2000)|NULL|用户可读错误|
|`internal_error_detail`|TEXT|NULL|内部错误，严格限权|
|`retry_count`|INT|NOT NULL DEFAULT 0|系统重试次数|
|`cancellable`|TINYINT(1)|NOT NULL DEFAULT 1|是否可取消|
|`deadline_at`|DATETIME(3)|NULL|任务硬超时|
|`started_at`|DATETIME(3)|NULL|开始时间|
|`completed_at`|DATETIME(3)|NULL|完成时间|
|`cancelled_at`|DATETIME(3)|NULL|取消时间|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|
|`deleted_at`|DATETIME(3)|NULL|用户侧软删除|
|`version`|INT|NOT NULL DEFAULT 0|乐观锁|

核心索引：

- `(user_id, status, created_at)`
- `(owner_type, owner_id, task_type, created_at)`
- `(status, deadline_at)`
- `(billing_status, updated_at)`
- `(billing_task_id)`

第一版数据库约束/应用校验：`task_type` 不允许 `edit`。

#### 3.6.5 `task_inputs`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|输入 ID|
|`task_id`|CHAR(26)|FK, INDEX|任务|
|`asset_id`|CHAR(26)|FK, INDEX|输入素材|
|`input_role`|VARCHAR(32)|NOT NULL|`primary/reference/logo`|
|`sort_order`|INT|NOT NULL DEFAULT 0|排序|
|`snapshot_json`|JSON|NULL|提交时素材快照|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|

#### 3.6.6 `task_outputs`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|输出 ID|
|`task_id`|CHAR(26)|FK, INDEX|任务|
|`asset_id`|CHAR(26)|FK, INDEX|结果素材|
|`output_type`|VARCHAR(20)|NOT NULL|`image/video/preview`|
|`sort_order`|INT|NOT NULL DEFAULT 0|排序|
|`provider_output_ref`|VARCHAR(256)|NULL|供应商结果引用|
|`review_status`|VARCHAR(20)|NOT NULL|内容审核状态|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|

#### 3.6.7 `task_attempts`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|尝试 ID|
|`task_id`|CHAR(26)|FK, INDEX|任务|
|`attempt_no`|INT|NOT NULL|第几次|
|`provider_code`|VARCHAR(32)|NOT NULL|供应商|
|`provider_model`|VARCHAR(128)|NULL|模型|
|`status`|VARCHAR(20)|NOT NULL|`submitted/running/succeeded/failed`|
|`request_snapshot_json`|JSON|NULL|脱敏后的请求|
|`error_code`|VARCHAR(64)|NULL|错误码|
|`error_message`|VARCHAR(1000)|NULL|错误|
|`started_at`|DATETIME(3)|NULL|开始|
|`finished_at`|DATETIME(3)|NULL|结束|
|`created_at`|DATETIME(3)|NOT NULL|创建|

唯一键：`(task_id, attempt_no)`。

#### 3.6.8 `provider_task_records`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|记录 ID|
|`task_id`|CHAR(26)|FK, INDEX|本地任务|
|`attempt_id`|CHAR(26)|FK, INDEX|尝试|
|`provider_code`|VARCHAR(32)|NOT NULL|供应商|
|`provider_task_id`|VARCHAR(128)|NOT NULL|供应商任务 ID|
|`provider_status`|VARCHAR(64)|NULL|原始状态|
|`callback_event_id`|VARCHAR(128)|NULL|最近事件|
|`last_response_json`|JSON|NULL|脱敏响应|
|`last_polled_at`|DATETIME(3)|NULL|轮询时间|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|

唯一键：`(provider_code, provider_task_id)`。

#### 3.6.9 `task_events`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|BIGINT UNSIGNED|PK AUTO_INCREMENT|事件序号|
|`task_id`|CHAR(26)|INDEX|任务|
|`event_type`|VARCHAR(64)|NOT NULL|事件类型|
|`from_status`|VARCHAR(24)|NULL|原状态|
|`to_status`|VARCHAR(24)|NULL|新状态|
|`progress`|TINYINT UNSIGNED|NULL|进度|
|`payload_json`|JSON|NULL|附加信息|
|`request_id`|VARCHAR(64)|NULL|请求链路|
|`created_at`|DATETIME(3)|NOT NULL|事件时间|

此表追加写，不更新历史事件。

### 3.7 下载、交付与通知

#### 3.7.1 `download_packages`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|下载包 ID|
|`user_id`|CHAR(26)|FK, INDEX|用户|
|`source_type`|VARCHAR(20)|NOT NULL|`task/project/selection`|
|`source_id`|CHAR(26)|NULL|来源 ID|
|`asset_ids_json`|JSON|NOT NULL|素材列表|
|`status`|VARCHAR(20)|NOT NULL|`pending/processing/ready/failed/expired`|
|`storage_key`|VARCHAR(1024)|NULL|ZIP 对象|
|`download_url`|VARCHAR(1024)|NULL|短期地址缓存|
|`expire_at`|DATETIME(3)|NULL|过期时间|
|`error_message`|VARCHAR(1000)|NULL|错误|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|

#### 3.7.2 `delivery_requests`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|需求 ID|
|`request_no`|VARCHAR(64)|UNIQUE|受理编号|
|`user_id`|CHAR(26)|FK, INDEX|提交人|
|`enterprise_id`|CHAR(26)|NULL, INDEX|企业|
|`title`|VARCHAR(160)|NOT NULL|标题|
|`description`|TEXT|NOT NULL|需求说明|
|`usage_scene`|VARCHAR(64)|NOT NULL|用途|
|`style_tags_json`|JSON|NULL|风格|
|`expected_delivery_at`|DATETIME(3)|NULL|期望交付|
|`enterprise_name`|VARCHAR(160)|NULL|企业名称快照|
|`contact_name`|VARCHAR(60)|NOT NULL|联系人|
|`contact_phone_ciphertext`|VARBINARY(512)|NOT NULL|电话|
|`contact_email_ciphertext`|VARBINARY(512)|NULL|邮箱|
|`status`|VARCHAR(32)|NOT NULL|交付状态|
|`assigned_to`|CHAR(26)|NULL|内部负责人|
|`submitted_at`|DATETIME(3)|NOT NULL|提交时间|
|`delivered_at`|DATETIME(3)|NULL|交付时间|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|

#### 3.7.3 `delivery_request_assets`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|关联 ID|
|`request_id`|CHAR(26)|FK, INDEX|交付需求|
|`asset_id`|CHAR(26)|FK, INDEX|素材|
|`asset_role`|VARCHAR(32)|NOT NULL|`reference/logo/brand_package/result`|
|`sort_order`|INT|NOT NULL DEFAULT 0|排序|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|

#### 3.7.4 `delivery_status_logs`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|日志 ID|
|`request_id`|CHAR(26)|FK, INDEX|交付需求|
|`from_status`|VARCHAR(32)|NULL|原状态|
|`to_status`|VARCHAR(32)|NOT NULL|新状态|
|`note`|VARCHAR(2000)|NULL|处理说明|
|`operator_type`|VARCHAR(20)|NOT NULL|`user/staff/system`|
|`operator_user_id`|CHAR(26)|NULL|操作人|
|`created_at`|DATETIME(3)|NOT NULL|变更时间|

#### 3.7.5 `enterprise_leads`

|字段|类型|说明|
|---|---|---|
|`id`|CHAR(26) PK|线索 ID|
|`source`|VARCHAR(32)|来源页面|
|`user_id`|CHAR(26) NULL|已登录用户|
|`enterprise_name`|VARCHAR(160) NULL|企业|
|`contact_name`|VARCHAR(60)|联系人|
|`contact_phone_ciphertext`|VARBINARY(512)|电话|
|`contact_email_ciphertext`|VARBINARY(512) NULL|邮箱|
|`requirement`|TEXT|需求|
|`preferred_contact_time`|VARCHAR(64) NULL|联系时间|
|`status`|VARCHAR(20)|`new/contacted/qualified/closed`|
|`assigned_to`|CHAR(26) NULL|负责人|
|`created_at/updated_at`|DATETIME(3)|时间|

#### 3.7.6 `notifications`

|字段|类型|说明|
|---|---|---|
|`id`|CHAR(26) PK|通知 ID|
|`user_id`|CHAR(26), INDEX|用户|
|`notification_type`|VARCHAR(32)|类型|
|`title`|VARCHAR(160)|标题|
|`content`|VARCHAR(2000)|内容|
|`biz_type`|VARCHAR(32) NULL|关联业务类型|
|`biz_id`|CHAR(26) NULL|关联业务 ID|
|`action_url`|VARCHAR(1024) NULL|跳转地址|
|`read_at`|DATETIME(3) NULL|已读时间|
|`created_at`|DATETIME(3)|创建时间|

索引：`(user_id, read_at, created_at)`。

### 3.8 可靠事件与审计

#### 3.8.1 `outbox_events`

|字段|类型|说明|
|---|---|---|
|`id`|CHAR(26) PK|事件 ID|
|`aggregate_type`|VARCHAR(32)|聚合类型|
|`aggregate_id`|CHAR(26)|聚合 ID|
|`event_type`|VARCHAR(64)|事件类型|
|`payload_json`|JSON|事件内容|
|`status`|VARCHAR(20)|`pending/publishing/published/failed`|
|`retry_count`|INT|重试次数|
|`next_retry_at`|DATETIME(3) NULL|下次重试|
|`published_at`|DATETIME(3) NULL|发布时间|
|`created_at/updated_at`|DATETIME(3)|时间|

索引：`(status, next_retry_at)`。

#### 3.8.2 `operation_audit_logs`

|字段|类型|说明|
|---|---|---|
|`id`|BIGINT UNSIGNED PK|日志 ID|
|`operator_type`|VARCHAR(20)|用户/管理员/系统|
|`operator_id`|CHAR(26) NULL|操作者|
|`action`|VARCHAR(64)|操作|
|`resource_type`|VARCHAR(32)|资源类型|
|`resource_id`|CHAR(26) NULL|资源 ID|
|`before_json`|JSON NULL|变更前|
|`after_json`|JSON NULL|变更后|
|`ip_address`|VARCHAR(45) NULL|IP|
|`user_agent`|VARCHAR(512) NULL|客户端|
|`request_id`|VARCHAR(64) NULL|请求 ID|
|`created_at`|DATETIME(3)|时间|

## 4. 积分数据库

积分数据库不得创建 `users`、`enterprises`、`plans`、`payment_orders`、`generation_tasks` 等业务表。

### 4.1 `credit_accounts`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|积分账户 ID|
|`owner_type`|VARCHAR(20)|NOT NULL|`user/enterprise`|
|`owner_id`|CHAR(26)|NOT NULL|业务库外部所有者 ID|
|`currency`|VARCHAR(16)|NOT NULL DEFAULT `CREDIT`|积分币种|
|`total_balance`|DECIMAL(18,4)|NOT NULL DEFAULT 0|总余额|
|`locked_balance`|DECIMAL(18,4)|NOT NULL DEFAULT 0|冻结余额|
|`available_balance`|DECIMAL(18,4)|生成列或应用维护|可用余额|
|`status`|VARCHAR(20)|NOT NULL|`active/frozen/closed`|
|`version`|BIGINT|NOT NULL DEFAULT 0|并发版本|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|

约束：

- 唯一键 `(owner_type, owner_id, currency)`。
- `total_balance >= 0`。
- `locked_balance >= 0`。
- `locked_balance <= total_balance`。
- `available_balance = total_balance - locked_balance`。

### 4.2 `credit_grants`

每次赠送或充值形成独立积分批次，用于先进先出和有效期管理。

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|积分批次 ID|
|`account_id`|CHAR(26)|FK, INDEX|账户|
|`grant_type`|VARCHAR(24)|NOT NULL|`register/plan/recharge/activity/adjustment/refund`|
|`original_points`|DECIMAL(18,4)|NOT NULL|原始积分|
|`remaining_points`|DECIMAL(18,4)|NOT NULL|剩余积分|
|`locked_points`|DECIMAL(18,4)|NOT NULL DEFAULT 0|已冻结积分|
|`biz_type`|VARCHAR(32)|NOT NULL|来源业务类型|
|`biz_id`|VARCHAR(64)|NOT NULL|来源业务 ID|
|`source_order_id`|VARCHAR(64)|NULL|业务支付订单 ID|
|`status`|VARCHAR(20)|NOT NULL|`active/consumed/expired/reversed`|
|`effective_at`|DATETIME(3)|NOT NULL|生效时间|
|`expire_at`|DATETIME(3)|NULL|过期时间|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|
|`version`|INT|NOT NULL DEFAULT 0|乐观锁|

索引：

- `(account_id, status, expire_at, effective_at)`
- `(biz_type, biz_id)`
- `(source_order_id)`

约束：

- `original_points > 0`
- `remaining_points >= 0`
- `locked_points >= 0`
- `remaining_points + locked_points <= original_points`

### 4.3 `billing_tasks`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|账务任务 ID|
|`account_id`|CHAR(26)|FK, INDEX|积分账户|
|`biz_type`|VARCHAR(32)|NOT NULL|如 `generation_task`|
|`biz_id`|VARCHAR(64)|NOT NULL|业务库任务 ID|
|`estimated_points`|DECIMAL(18,4)|NOT NULL|预估积分|
|`frozen_points`|DECIMAL(18,4)|NOT NULL DEFAULT 0|冻结积分|
|`settled_points`|DECIMAL(18,4)|NOT NULL DEFAULT 0|结算积分|
|`refunded_points`|DECIMAL(18,4)|NOT NULL DEFAULT 0|退款/释放积分|
|`status`|VARCHAR(24)|NOT NULL|账务状态|
|`pricing_snapshot_json`|JSON|NULL|规则版本等非业务敏感快照|
|`freeze_idempotency_key`|VARCHAR(128)|NOT NULL|冻结幂等键|
|`settle_idempotency_key`|VARCHAR(128)|NULL|结算幂等键|
|`refund_idempotency_key`|VARCHAR(128)|NULL|退款幂等键|
|`lock_expire_at`|DATETIME(3)|NULL|冻结过期时间|
|`frozen_at`|DATETIME(3)|NULL|冻结时间|
|`settled_at`|DATETIME(3)|NULL|结算时间|
|`refunded_at`|DATETIME(3)|NULL|退款时间|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|
|`version`|INT|NOT NULL DEFAULT 0|乐观锁|

唯一键：

- `(biz_type, biz_id)`：一个业务任务只对应一个正式账务任务。
- `freeze_idempotency_key`。

状态：

```text
created
frozen
partially_settled
settled
refunding
refunded
cancelled
failed
```

### 4.4 `billing_locks`

一条账务任务可能从多个积分批次冻结，因此按批次记录。

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|冻结明细 ID|
|`billing_task_id`|CHAR(26)|FK, INDEX|账务任务|
|`account_id`|CHAR(26)|FK, INDEX|账户|
|`grant_id`|CHAR(26)|FK, INDEX|积分批次|
|`locked_points`|DECIMAL(18,4)|NOT NULL|冻结积分|
|`settled_points`|DECIMAL(18,4)|NOT NULL DEFAULT 0|已结算|
|`released_points`|DECIMAL(18,4)|NOT NULL DEFAULT 0|已释放|
|`status`|VARCHAR(20)|NOT NULL|`active/settled/released/expired`|
|`expire_at`|DATETIME(3)|NULL|冻结过期|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|

唯一键：`(billing_task_id, grant_id)`。

### 4.5 `credit_transactions`

不可变账务流水，只允许 INSERT，不允许 UPDATE/DELETE。

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|流水 ID|
|`account_id`|CHAR(26)|FK, INDEX|账户|
|`grant_id`|CHAR(26)|NULL, INDEX|积分批次|
|`billing_task_id`|CHAR(26)|NULL, INDEX|账务任务|
|`transaction_type`|VARCHAR(24)|INDEX|流水类型|
|`direction`|VARCHAR(8)|NOT NULL|`credit/debit/lock/unlock`|
|`points`|DECIMAL(18,4)|NOT NULL|绝对值|
|`total_balance_before`|DECIMAL(18,4)|NOT NULL|变动前总余额|
|`total_balance_after`|DECIMAL(18,4)|NOT NULL|变动后总余额|
|`locked_balance_before`|DECIMAL(18,4)|NOT NULL|变动前冻结|
|`locked_balance_after`|DECIMAL(18,4)|NOT NULL|变动后冻结|
|`available_balance_after`|DECIMAL(18,4)|NOT NULL|变动后可用|
|`biz_type`|VARCHAR(32)|NOT NULL|业务类型|
|`biz_id`|VARCHAR(64)|NOT NULL|业务 ID|
|`source_order_id`|VARCHAR(64)|NULL|订单引用|
|`ref_transaction_id`|CHAR(26)|NULL|冲正/退款原流水|
|`description_code`|VARCHAR(64)|NOT NULL|描述代码|
|`metadata_json`|JSON|NULL|最小必要元数据|
|`created_at`|DATETIME(3)|NOT NULL|发生时间|

流水类型：

```text
grant
recharge
freeze
settle
release
refund
expire
adjust_credit
adjust_debit
reverse
```

索引：

- `(account_id, created_at)`
- `(account_id, transaction_type, created_at)`
- `(biz_type, biz_id)`
- `(source_order_id)`

### 4.6 `idempotency_keys`

|字段|类型|约束|说明|
|---|---|---|---|
|`id`|CHAR(26)|PK|记录 ID|
|`caller`|VARCHAR(64)|NOT NULL|调用方|
|`operation`|VARCHAR(64)|NOT NULL|操作|
|`idempotency_key`|VARCHAR(128)|NOT NULL|幂等键|
|`request_hash`|CHAR(64)|NOT NULL|请求摘要|
|`status`|VARCHAR(20)|NOT NULL|`processing/succeeded/failed`|
|`resource_type`|VARCHAR(32)|NULL|结果资源类型|
|`resource_id`|CHAR(26)|NULL|结果资源 ID|
|`response_json`|JSON|NULL|成功响应缓存|
|`error_code`|VARCHAR(64)|NULL|失败码|
|`expires_at`|DATETIME(3)|NULL|幂等记录过期|
|`created_at`|DATETIME(3)|NOT NULL|创建时间|
|`updated_at`|DATETIME(3)|NOT NULL|更新时间|

唯一键：`(caller, operation, idempotency_key)`。

### 4.7 `ledger_outbox_events`

|字段|类型|说明|
|---|---|---|
|`id`|CHAR(26) PK|事件 ID|
|`aggregate_type`|VARCHAR(32)|账户/账务任务|
|`aggregate_id`|CHAR(26)|聚合 ID|
|`event_type`|VARCHAR(64)|事件类型|
|`payload_json`|JSON|事件内容|
|`status`|VARCHAR(20)|发布状态|
|`retry_count`|INT|重试次数|
|`next_retry_at`|DATETIME(3) NULL|下次重试|
|`published_at`|DATETIME(3) NULL|发布时间|
|`created_at/updated_at`|DATETIME(3)|时间|

### 4.8 `account_daily_snapshots`

|字段|类型|说明|
|---|---|---|
|`id`|CHAR(26) PK|快照 ID|
|`account_id`|CHAR(26), INDEX|账户|
|`snapshot_date`|DATE|日期|
|`opening_total`|DECIMAL(18,4)|期初总额|
|`closing_total`|DECIMAL(18,4)|期末总额|
|`closing_locked`|DECIMAL(18,4)|期末冻结|
|`credit_points`|DECIMAL(18,4)|当日增加|
|`debit_points`|DECIMAL(18,4)|当日消耗|
|`transaction_count`|INT|流水数|
|`checksum`|CHAR(64)|校验摘要|
|`created_at`|DATETIME(3)|生成时间|

唯一键：`(account_id, snapshot_date)`。

### 4.9 `reconciliation_issues`

|字段|类型|说明|
|---|---|---|
|`id`|CHAR(26) PK|异常 ID|
|`issue_type`|VARCHAR(64)|异常类型|
|`account_id`|CHAR(26) NULL|账户|
|`biz_type`|VARCHAR(32) NULL|业务类型|
|`biz_id`|VARCHAR(64) NULL|业务 ID|
|`expected_json`|JSON NULL|预期|
|`actual_json`|JSON NULL|实际|
|`status`|VARCHAR(20)|`open/processing/resolved/ignored`|
|`resolution_note`|VARCHAR(2000) NULL|处理说明|
|`created_at/updated_at`|DATETIME(3)|时间|
|`resolved_at`|DATETIME(3) NULL|解决时间|

## 5. 双库映射

|业务库对象|业务库字段|积分库对象|积分库字段|
|---|---|---|---|
|用户|`users.id`|账户|`credit_accounts.owner_id`|
|用户积分账户|`users.credit_account_id`|账户|`credit_accounts.id`|
|企业|`enterprises.id`|账户|`credit_accounts.owner_id`|
|生成任务|`generation_tasks.id`|账务任务|`billing_tasks.biz_id`|
|账务引用|`generation_tasks.billing_task_id`|账务任务|`billing_tasks.id`|
|支付订单|`payment_orders.id`|积分批次/流水|`source_order_id`|

规则：

- 以上均为逻辑引用，不建立数据库外键。
- 积分库中的 `owner_id/biz_id/source_order_id` 使用字符串保存，不能假设业务库主键格式永远不变。
- 业务后端负责检查引用对象存在性。

## 6. 关键事务

### 6.1 注册赠送

业务库事务：

1. 插入 `users`。
2. 插入 `auth_sessions`。
3. 插入 `outbox_events(user.registered)`。

积分库事务：

1. 插入或读取 `credit_accounts`。
2. 插入 `credit_grants`。
3. 更新账户余额。
4. 插入 `credit_transactions(grant)`。
5. 插入 `ledger_outbox_events`。

业务库收到结果后更新 `users.credit_account_id/credit_account_status`。

### 6.2 任务冻结

业务库：

1. 锁定并使用 `task_quotes`。
2. 插入 `generation_tasks(status=billing_pending)`。

积分库：

1. 锁定 `credit_accounts`。
2. 按过期时间优先选择可用 `credit_grants`。
3. 插入 `billing_tasks`。
4. 更新批次锁定积分。
5. 插入 `billing_locks`。
6. 更新账户冻结余额。
7. 插入 `credit_transactions(freeze)`。

成功后业务库更新 `billing_task_id/billing_status` 并投递 AI 任务。

### 6.3 成功结算

积分库单事务：

1. 锁定账务任务、账户和冻结明细。
2. 从批次剩余积分中正式扣除。
3. 释放多冻结的差额。
4. 更新 `billing_tasks`。
5. 更新 `billing_locks`。
6. 更新账户总余额和冻结余额。
7. 写入 `settle` 和必要的 `release` 流水。

业务库更新任务的 `settled_credits/billing_status`。

### 6.4 失败退款

积分库不删除冻结记录：

1. 将冻结积分释放回原批次。
2. 更新账户冻结余额。
3. 更新账务任务为 `refunded`。
4. 写入 `refund/release` 流水。

## 7. 索引与性能

- 所有列表必须使用“所有者 + 状态/类型 + 时间”组合索引。
- 禁止只按低区分度状态字段建立单列索引后依赖全表扫描。
- 任务和流水大表优先使用基于时间和 ID 的游标分页。
- `task_events/credit_transactions/operation_audit_logs` 预计增长快，应预留按月分区或归档方案。
- JSON 字段只保存低频变化配置；高频查询字段必须拆成普通列。
- URL 和大段供应商原始响应不要在列表查询中读取。

## 8. 数据生命周期

|数据|建议策略|
|---|---|
|用户和企业|注销后软删除，敏感字段按合规要求匿名化|
|验证码|30-90 天后清理|
|登录会话|失效后保留 180 天审计，再清理|
|上传临时文件|24 小时未绑定则清理|
|任务和结果|按产品约定保留，删除采用软删除 + 延迟物理清理|
|支付订单和回调|长期保存并按法规归档|
|积分流水|不可删除，长期保存|
|操作审计|至少保存 1 年，账务相关更长|

## 9. 备份与恢复

- 两套数据库分别备份，不允许只恢复其中一套后直接开放写入。
- 每日全量备份 + Binlog 增量恢复。
- 定期执行恢复演练。
- 恢复后先运行双库对账，再解除只读。
- 积分库恢复必须校验账户余额、积分批次、流水和快照 checksum。

## 10. 数据库验收

必须验证：

1. 业务库不存在积分正式流水表。
2. 积分库不存在用户资料、套餐、支付订单、任务配置和素材表。
3. 同一用户只能有一个同币种积分账户。
4. 余额和冻结余额不能为负。
5. 同一业务任务不能重复冻结。
6. 同一支付订单不能重复入账。
7. 结算和退款并发时状态与余额正确。
8. 流水前后余额可以完整重放并对上账户余额。
9. 业务任务和账务任务可通过 ID 完整对账。
10. 第一版任务表不接受 `edit` 类型。

## 11. 待确认项

- 用户积分账户与企业共享账户是否同时支持。
- 新人赠送数量、有效期和积分批次规则。
- 套餐赠送积分是每月重置还是逐月形成批次。
- 退款积分是否恢复原过期时间。
- 支付退款后已消费积分的追偿规则。
- 任务部分成功时按成功张数结算还是整单结算。
- 图片、视频和任务数据的正式保留周期。
